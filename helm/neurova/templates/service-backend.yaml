apiVersion: v1
kind: Service
metadata:
  name: {{ include "neurova.fullname" . }}-backend
  labels:
    {{- include "neurova.backend.selectorLabels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: {{ .Values.backend.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "neurova.backend.selectorLabels" . | nindent 4 }}