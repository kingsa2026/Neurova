# 资源型修复登记台账（四路代理"发现但未修"项汇总）

> 日期：2026-09-11
> 来源：资源型修复当日四路代理的「发现但未修」登记（渠道域 / API 端点域 / scheduler+camofox / 前端），主线逐条复核定性。
> 用途：后续修复排期台账。已修 19 项见 docs/资源型Bug扫描报告_2026-09-11.md 修复状态头。

---

## 一、建议近期修（真实缺陷，有触发路径或已在产生成本）

| # | 定位 | 问题 | 来源域 | 建议优先级 |
|---|---|---|---|---|
| 1 | `neurova/channels/discord.py`、`qqbot.py` 缺 `connect`+`disconnect`；`websocket.py`、`mqtt.py` 缺 `connect` | 四个适配器 `TypeError: Can't instantiate abstract class`——`POST /channels-config/{type}` 保存/测试对 discord/qqbot/mqtt 在实例化处即 400（Gen2 契约修复后 health_check 问题被 400 前置掩盖；QQAdapter 已可实例化且 health_check 恢复） | 渠道 | **P1**（渠道功能级，点按钮即触发；补方法属设计决策需定实现语义） |
| 2 | `neurova/api/endpoints/neurflow_api.py:1423` | `_DEBUG_SESSIONS` 每调试执行永久驻留，无淘汰/LRU（报告 P2-6 前半，当时任务只指派了 create_task 引用后半） | API | P2（净修小） |
| 3 | `neurova/api/endpoints/mobile_pairing.py:145` | `_cancelled_sessions: Dict[str, bool]` 只增不清（与已修的三表同族，报告漏列） | API | P2（净修小） |
| 4 | `neurova/channels/mobile_pairing.py` | 与 `api/endpoints/mobile_pairing.py` 双实现未合并——`test_mobile_pairing_p0::TestDuplicateImplementation` 预存红，契约漂移风险 | 渠道 | P2 |
| 5 | `neurova/tool_executor.py` 身份注入 | 契约错位：读到 auto-mock 的 `current_user_id` 而非 `_current_user_id`——`test_camofox_isolation` 2 个预存失败的根因；**放大视角**：同契约其他消费方应一并排查 | scheduler/camofox | P2 |
| 6 | `neurova/api/endpoints/media.py` 下载响应 | `Content-Disposition` 文件名未做 RFC5987 编码，非 ASCII 文件名（中文）下载头异常 | API | P2 |
| 7 | `neurova/agent/scheduler.py` 重试链结构 | `_handle_retry` 在初始 execute_task 帧内 await 整条重试链：内层 execution 的 finally 先入历史（history append 顺序与执行顺序相反）、外层 execution 的 `ended_at/duration` 与 `_running_executions` 注册跨越整个重试链。链深现已被 max_attempts 封顶，彻底解法是 fire-and-forget 重试任务（结构性改动） | scheduler | P2/延后 |

## 二、潜伏项（当前无触发路径，接线/未来调用方即升级）

| # | 定位 | 问题 | 触发条件 |
|---|---|---|---|
| 8 | `neurova/channels/websocket.py:218,410` | Gen1 sync 接口残留：`_init_connection` 与 sync `send_message` 仍在捕获 loop 上 `run_until_complete`（与 P1-8 同族，运行中循环上调用即 RuntimeError）；该适配器整体未接线 | 接线 websocket 渠道时需统一 async 化 |
| 9 | `neurova/channels/sip.py` | SIPAdapter 同 Gen2 契约缺口（无 super().__init__，且缺 connect/disconnect/send_message） | 启用/测试 sip 渠道 |
| 10 | `NeurUI/src/stores/chat.ts setMessages` | 直接替换数组不 revoke 旧消息 blob——现有调用方均先 clearMessages 或传后端映射（无 blob URL）；未来调用方直设客户端活消息会泄漏 | 未来新调用方 |
| 11 | `NeurUI` ImagePreviewPanel/AudioPreviewPanel | 对 `tab.data.url` 无条件 revoke 非 dock 自建 URL——当前所有调用方合规，未来传外来 blob URL 会把消息内 blob 误撤（回归点） | 未来新调用方传 blob: |
| 12 | `NeurUI/src/pages/ChatPage.vue:1934` | dock 内联图 `openImageTab(img.src)`：markdown 内联图若将来嵌 `blob:` URL 会被面板卸载 revoke；当前内联图均为服务端 URL，无活路径 | 消息内容嵌 blob URL |

## 三、良性/按现设计不改（避免过度工程）

| # | 定位 | 说明 |
|---|---|---|
| 13 | `qq.py:334`、`qqbot.py:301`、`discord.py:232`、`sip.py:339`、`qclaw.py:225` 的 `receive_message` | 零消费口死方法，但均为返回 None 的 stub、无队列堆积，无泄漏风险（可与死代码清理批一并删） |
| 14 | `feishu.py:205-235` `_download_media_bytes` 同步 requests | 运行在 lark SDK ws 回调线程，不占事件循环，按现设计不阻断主循环 |
| 15 | camofox drain 线程不受 stop() join | 依赖杀进程后管道 EOF 自然退出——已核实良性 |
| 16 | `camofox_supervisor.py` `_kill_process` 用 `asyncio.get_event_loop()` | 弃用模式告警，`asyncio.run` 下仍正常；可顺带现代化但非缺陷 |
| 17 | `media.py:151` `_get_media_manager()` | 死代码（无路由调用 MediaManager），可随死代码清理批删除 |
| 18 | `openImageTab` 去重键 | 自建 URL 后同一附件重复点击生成新 tab（不回焦旧 tab）——轻微 UX 变化，非泄漏 |

---

**修复建议**：#2/#3/#6 三项净修小可并入下一批随手修；#1/#4/#5 各需半日内完成；#7 与潜伏项挂账待接线/重构批次。
