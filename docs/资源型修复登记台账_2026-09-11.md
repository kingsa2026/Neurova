# 资源型修复登记台账（四路代理"发现但未修"项汇总）

> 日期：2026-09-11
> 来源：资源型修复当日四路代理的「发现但未修」登记（渠道域 / API 端点域 / scheduler+camofox / 前端），主线逐条复核定性。
> 用途：后续修复排期台账。已修 19 项见 docs/资源型Bug扫描报告_2026-09-11.md 修复状态头。

---

> **✅ 修复状态（2026-09-11 当日二次批次，台账 18 项全清）：**
> - **一档 7 项全修**：#1 Gen2 四适配器（discord/qqbot/websocket/mqtt）connect/disconnect 补齐——全部接真实路径、无 token/依赖缺失诚实失败，端点链路 connect→health_check→disconnect 打通；#2 `_DEBUG_SESSIONS` 改 LRU（上限 100，裸赋值路径同样受封顶）；#3 `_cancelled_sessions` 按流生命周期回收（**顺带根修一个真实行为 bug**：残留取消标记会误杀同会话后续新流）；#4 删除零消费死实现 channels/mobile_pairing.py（−259 行），端点级 8 用例迁入 tests/unit/api/test_mobile_pairing_endpoints.py，双实现预存红转绿；#5 tool_executor 身份注入读序根修（`_current_user_id` 优先，对齐 ：3054 规范契约），camofox_isolation 2 预存红转绿；#6 Content-Disposition RFC5987（`filename*=UTF-8''` + ASCII 回退 + 封住头注入面）；#7 重试链改 fire-and-forget（spawn 独立任务 + `_pending_retry_tasks` 持引用 + stop 取消 + wrapper drain 接线），history 顺序==执行顺序、外层计时只覆盖本链节点。
> - **二档 5 项全修**：#8 websocket Gen1 sync 残留消灭（run_until_complete 零命中 + `_event_loop` 清除 + 假成功路径删除）；#9 sip 诚实实现（契约三件 + 未实现警告 + 诚实 False，不虚构能力）；#10 setMessages 替换前 revoke（幂等）；#11 dock 两面板只 revoke 自建 URL（`createdBy` 所有权标记，全调用方归属核对）；#12 内联图路径契约锁存。
> - **三档 6 项处置**：#13 五处死 stub 删除；#14 feishu 下载核实本就带 timeout=15，防回归钉住；#15 drain 线程有界 join(2s)；#16 `_kill_process` 改 `asyncio.to_thread`（弃用 API 清零）；#17 media 死代码删除；#18 openImageTab 稳定去重键（file_id/原始 URL，重复点击回焦）。
> - **测试**：新增/迁移防回归约 60 用例全绿；渠道域 287→319；前端 1282→1300 + vue-tsc 零错误；统一回归 2656 passed，66F+31E **全部预存**（新覆盖的 tests/unit/agent 全目录与 tests/test_channels 经 worktree 基线对照失败数完全一致）。
> - **净 LOC（生产代码）**：渠道 ≈ −290（mobile_pairing 删除对冲）、API +48、scheduler/tool_executor/camofox +90、前端 +69 ≈ **净 −83**。
> - **本轮新登记（见第四节）**：7 条相邻问题。
> - ⚠️ 事故教训沿用：本轮全部测试保持 env 隔离（HOME/USERPROFILE/零真实网络），零真实文件触碰。

---

## 一、建议近期修（真实缺陷，有触发路径或已在产生成本）——✅ 已全部修复

| # | 定位 | 问题 | 来源域 | 建议优先级 |
|---|---|---|---|---|
| 1 | `neurova/channels/discord.py`、`qqbot.py` 缺 `connect`+`disconnect`；`websocket.py`、`mqtt.py` 缺 `connect` | 四个适配器 `TypeError: Can't instantiate abstract class`——`POST /channels-config/{type}` 保存/测试对 discord/qqbot/mqtt 在实例化处即 400（Gen2 契约修复后 health_check 问题被 400 前置掩盖；QQAdapter 已可实例化且 health_check 恢复） | 渠道 | **P1**（渠道功能级，点按钮即触发；补方法属设计决策需定实现语义） |
| 2 | `neurova/api/endpoints/neurflow_api.py:1423` | `_DEBUG_SESSIONS` 每调试执行永久驻留，无淘汰/LRU（报告 P2-6 前半，当时任务只指派了 create_task 引用后半） | API | P2（净修小） |
| 3 | `neurova/api/endpoints/mobile_pairing.py:145` | `_cancelled_sessions: Dict[str, bool]` 只增不清（与已修的三表同族，报告漏列） | API | P2（净修小） |
| 4 | `neurova/channels/mobile_pairing.py` | 与 `api/endpoints/mobile_pairing.py` 双实现未合并——`test_mobile_pairing_p0::TestDuplicateImplementation` 预存红，契约漂移风险 | 渠道 | P2 |
| 5 | `neurova/tool_executor.py` 身份注入 | 契约错位：读到 auto-mock 的 `current_user_id` 而非 `_current_user_id`——`test_camofox_isolation` 2 个预存失败的根因；**放大视角**：同契约其他消费方应一并排查 | scheduler/camofox | P2 |
| 6 | `neurova/api/endpoints/media.py` 下载响应 | `Content-Disposition` 文件名未做 RFC5987 编码，非 ASCII 文件名（中文）下载头异常 | API | P2 |
| 7 | `neurova/agent/scheduler.py` 重试链结构 | `_handle_retry` 在初始 execute_task 帧内 await 整条重试链：内层 execution 的 finally 先入历史（history append 顺序与执行顺序相反）、外层 execution 的 `ended_at/duration` 与 `_running_executions` 注册跨越整个重试链。链深现已被 max_attempts 封顶，彻底解法是 fire-and-forget 重试任务（结构性改动） | scheduler | P2/延后 |

## 二、潜伏项（当前无触发路径，接线/未来调用方即升级）

| # | 定位 | 问题 | 触发条件 |
|---|---|---|---|
| 8 | `neurova/channels/websocket.py:218,410` | Gen1 sync 接口残留：`_init_connection` 与 sync `send_message` 仍在捕获 loop 上 `run_until_complete`（与 P1-8 同族，运行中循环上调用即 RuntimeError）；该适配器整体未接线 | 接线 websocket 渠道时需统一 async 化 |
| 9 | `neurova/channels/sip.py` | SIPAdapter 同 Gen2 契约缺口（无 super().__init__，且缺 connect/disconnect/send_message） | 启用/测试 sip 渠道 |
| 10 | `NeurUI/src/stores/chat.ts setMessages` | 直接替换数组不 revoke 旧消息 blob——现有调用方均先 clearMessages 或传后端映射（无 blob URL）；未来调用方直设客户端活消息会泄漏 | 未来新调用方 |
| 11 | `NeurUI` ImagePreviewPanel/AudioPreviewPanel | 对 `tab.data.url` 无条件 revoke 非 dock 自建 URL——当前所有调用方合规，未来传外来 blob URL 会把消息内 blob 误撤（回归点） | 未来新调用方传 blob: |
| 12 | `NeurUI/src/pages/ChatPage.vue:1934` | dock 内联图 `openImageTab(img.src)`：markdown 内联图若将来嵌 `blob:` URL 会被面板卸载 revoke；当前内联图均为服务端 URL，无活路径 | 消息内容嵌 blob URL |

## 三、良性/按现设计不改（避免过度工程）

| # | 定位 | 说明 |
|---|---|---|
| 13 | `qq.py:334`、`qqbot.py:301`、`discord.py:232`、`sip.py:339`、`qclaw.py:225` 的 `receive_message` | 零消费口死方法，但均为返回 None 的 stub、无队列堆积，无泄漏风险（可与死代码清理批一并删） |
| 14 | `feishu.py:205-235` `_download_media_bytes` 同步 requests | 运行在 lark SDK ws 回调线程，不占事件循环，按现设计不阻断主循环 |
| 15 | camofox drain 线程不受 stop() join | 依赖杀进程后管道 EOF 自然退出——已核实良性 |
| 16 | `camofox_supervisor.py` `_kill_process` 用 `asyncio.get_event_loop()` | 弃用模式告警，`asyncio.run` 下仍正常；可顺带现代化但非缺陷 |
| 17 | `media.py:151` `_get_media_manager()` | 死代码（无路由调用 MediaManager），可随死代码清理批删除 |
| 18 | `openImageTab` 去重键 | 自建 URL 后同一附件重复点击生成新 tab（不回焦旧 tab）——轻微 UX 变化，非泄漏 |

---

**修复建议**：#2/#3/#6 三项净修小可并入下一批随手修；#1/#4/#5 各需半日内完成；#7 与潜伏项挂账待接线/重构批次。
（2026-09-11 二次批次：以上已全部完成，见文首修复状态。）

---

## 四、二次批次新登记（修复过程中新发现的相邻问题）

| # | 定位 | 问题 | 建议 |
|---|---|---|---|
| N1 | `neurova/channels/sip.py` | 预存假成功残留：`_init_production_mode`/`_send_production_audio` 无条件 return True、`_init_dev_mode` 在 pyVoIP 缺失时"模拟初始化"return True——仅经 `play_welcome_message` 可达（当前零外部触发） | 随 SIP 实装批次清理 |
| N2 | discord/qqbot/qq/websocket/mqtt 的 `send_message` | Gen1 签名错位：保留 `(self, message: UnifiedMessage)`（websocket 已 async 化），与基类 `async send_message(chat_id, content, ...)` 不同契约——manager 未来按基类签名直连会 TypeError | 统一签名或双方法（参照 qclaw 先例） |
| N3 | `neurova/agent/loops/base.py:200`、`post_chat_pipeline.py:616` | 只读 public `current_user_id` 别名——生产语义正确，但与 #5 旧序共享 MagicMock 影子风险 | 测试替身覆盖到时同步改读序 |
| N4 | `mobile_websocket` 单连接取消时序 | 同一 WS 连接的 `chat:cancel` 只能在流结束后被处理——#3 修复已使其无害（孤儿标记被消费），流中途取消需前端双连接 | 结构性，挂账 |
| N5 | `openCodeBlockTab` svg 代码块 | dock 预览显示"不可用"（ImagePreviewPanel 无 content 分支）——预存功能缺口，非资源型 | 功能批处理 |
| N6 | `NeurUI/src/composables/useStreamTTS.ts:142` | 播报被新音频打断时旧 Audio 永不触发 onended，单次泄漏一个 object URL——预存轻微 | 下批前端随手修 |
| N7 | `scheduler._execute_task_wrapper` | 每 job 新建事件循环为预存设计——#7 的 drain 依赖其存在，重构调度器时需一并评估 | 挂账 |

---

## 五、全量遗留终账（2026-09-11 收集，跨批次未修项汇总）

> 口径：两轮修复（19 项 + 台账 18 项）之后仍开放的资源/功能缺陷与潜伏项全景。前端 UI 两项（N5/N6）当日已修，其余为登记待排期。

> **✅ 终账修复状态（2026-09-11 第三批，B 档 11 项处置完毕）：**
> - **B-1 pydantic 兼容债**：根因=环境漂移（全局解释器 pydantic 1.10.26 vs requirements `>=2.5.0`）——对齐到 2.13.5 后 22 个失败中 20 个直接转绿，**代码零改动**；连带补装 requirements 已声明的 python-docx/python-pptx/striprtf。剩余 2 个 docx/pptx 失败为 lxml 在 Python 3.15.0a7 上的 C 插槽漂移（见 C 档环境族）。
> - **B-1 连带新真 bug（当场根修）**：`MemoryManager.close()` 空壳不关 `_persist_conn` 常驻 WAL 连接（3820cdd9 引入）+ agent_shutdown A-05 持有人清单漏 memory_manager——包导入回滚 rmtree 撞 `neurova_memories_persist.db` 句柄（WinError 32）。两处根修后 **test_agent_package_api 16/16 转绿**（原为当日台账预存失败）。
> - **B-2 环境声明**：补装 prometheus-client/Pillow/aiohttp/cryptography 并写入 requirements（带版本约束）；environment_setup 声明类转绿。**残余 3 失败 = Python 3.15.0a7 alpha 解释器的二进制不兼容族**（PIL slot 85 / lxml slot 84 / greenlet DLL）——健康环境是项目 `.venv`(3.12)，建议回归统一走 `.venv`，代码层不可修。
> - **B-3/B-4 渠道**：sip 四处假成功路径清零（诚实 False+警告，真实 pyVoIP 路径锁存）；Gen1 四适配器（discord/qqbot/qq/websocket）补基类签名 `send_message(chat_id, content, ...)`，旧 UnifiedMessage 路径更名 `_send_unified`；**同根因放大到 mqtt 也已收口**。渠道 319→345+ 绿。
> - **B-5**：loops/base.py:200 + post_chat_pipeline.py:616 身份读序对齐（`_current_user_id` 优先）。
> - **B-6**：mobile WS receive 循环 spawn 独立流任务（`_chat_tasks` 持引用）——同连接 `chat:cancel` 流中途生效、断开收尸、流中新 send 拒绝（chat_busy）。
> - **B-8**：context_pool 补 `_by_turn` 索引，`mark_turn_seen` 从全池 O(n) 降 O(k)，六处重排点同步。
> - **B-9**：SessionManager 加 (路径,mtime,size) 键 LRU 摘要缓存（上限 2000）+ `get_feedback_counts` 反馈聚合；console 统计端点不再 get_history 全量。存储格式零变更，S4/S3 未触碰。
> - **B-10**：召回改常驻单事件循环线程（`run_coroutine_threadsafe` + 超时返 None），M-13 语义不变，弃置线程堆积消除。
> - **B-11**：死代码删除/补界——tool_cache（−460）、auto_context_updater（−300）、anthropic `_SyncStreamBridge`（−33）、task_tracker `start_tracking` 死路径（−44）、subagent `_spawned` 有界淘汰（200）；同根因悬空引用 gemini_client 死方法一并删除。基建域净 **−757**。
> - **B-7 已单独立项**：详见 **docs/B7调度器事件循环统一立项_2026-09-11.md**（现状锚点/问题定性 P1-P4/三阶段方案/风险缓解/验收标准，预估 2 天，建议单代理 TDD 执行）。立项时新增两个休眠事实登记：AsyncIOScheduler 为死声明（:616 无实例化）、ProcessPoolExecutor 配置空转（add_job 从未指定 executor）。
> - **测试**：新增防回归约 60 用例全绿；统一回归（api/channels/memory/cognitive_layers/tools/context 全目录）与基线 worktree **逐条差分零新增失败**，且净消基线 4 个失败；前端 1306 绿 + vue-tsc 0。
> - **净 LOC**：≈ **−1100**（基建 −757、渠道 +126、session +177、mqtt/gemini/WAL 修复 +41、前端对齐 +71，测试不计）。
> - **新登记（第四节后续）**：①TaskTracker 同步路径剩余方法（update_progress 等 9 个）随 start_tracking 删除已不可达，下轮机械清理约 −150；②SessionManager 单例 `_sessions_dir` 被多个测试文件绝对路径改写、毒化顺序敏感测试（round_ops 7 失败为基线预存）——建议统一 fixture 规约；③`_handle_ws_message` 双路径（内联+拦截）待后续归一；④test_execution_events_stream::test_stream_user_isolation 顺序依赖波动（本批运行中转绿，未改动）。
>
> **✅ 登记项清尾批次（2026-09-11 第四批，上述登记 + 欠付项全清）：**
> - **TaskTracker 同步路径整体删除**（TaskInfo/TaskStatus/9 个同步方法/订阅器/清理线程，`_tasks` 写入口唯 start_tracking 已死）——neurova/core/task_tracker.py 重写为纯 asyncio 注册表（534→136 行），测试重写 9 绿含防复活锁存；ad-hoc tests/test_api/test_console_api.py 同步测试类一并摘除（保持可收集）。
> - **测试毒化根除**：test_p1_review_fixes.py:109 直赋值单例改 monkeypatch 自动恢复；全量顺序下 **round_ops 13/13 转绿**（基线预存 7 失败清零）。
> - **渠道诚实性清尾**：websocket `[模拟] return True`、mqtt `[MQTT模拟] return True`、sip TTS/STT 模拟数据——全部诚实失败（warning+False/None）+ 锁存测试。
> - **平台真实 message_id 回传**：discord/qqbot/qq `_send_unified` 改 Optional[str] 提取响应真实 ID（无 ID 回落本地生成），调用方真值语义逐一核查兼容；websocket 维持 bool（无回执）、mqtt 维持本地 id（paho mid 是可回收在途标识，非消息身份——决策记录在案）。
> - **mobile WS 双路径归一**：`_handle_ws_message` 删除 chat:send/chat:cancel 平行内联分支（B-6 后永不触达），拦截路径为唯一入口；handler_error 契约测试入口适配、断言不削弱。
> - **环境族终结验证**：.venv（3.12）下 environment_setup/knowledge_import/pending_memory/camofox_isolation **67/67 全绿**——全局 3.15a7 的 PIL/lxml/greenlet 残留失败确证为解释器 alpha 问题，**回归统一走 .venv 即全绿**，代码层零遗留。
> - 本批净 LOC ≈ **−380**（TaskTracker −398、诚实化/回传/归一 +39、requirements +6、测试迁移不计）。合并回归 1716 passed，仅剩 2 失败 = lxml-alpha 环境族（.venv 已证绿）。

### A. 已修（本轮 UI 对齐，2026-09-11）

| # | 定位 | 问题与修法 |
|---|---|---|
| A-1（原 N5） | `ImagePreviewPanel.vue` | svg 代码块 dock 预览恒空白（openCodeBlockTab 只带 content，三分支全不命中）。修法：面板自建 `image/svg+xml` blob URL 分支（所有权归面板统一释放；`<img>` 渲染 SVG 脚本不执行）；watch 源补 `content`（右 dock 面板不按 tab.id 重挂载，svg↔svg 切换原会残留旧图）。防回归 3 例。 |
| A-2（原 N6） | `useStreamTTS.ts createSpeechAnnouncer` | 播报被新音频打断时旧 Audio pause 后 onended 永不触发，每次打断泄漏一个 object URL（dispose 同）。修法：打断/销毁路径显式 revoke；自然播完路径不变。防回归 3 例（打断/dispose/幂等）。 |

### B. 后端遗留（登记未修，按优先级）

| # | 定位 | 问题 | 触发/影响 | 建议 |
|---|---|---|---|---|
| B-1 | pydantic 单源兼容债（governance.py:330 `model_dump`、experience `model_validate` 等 ~22 个失败测试） | 代码混用 pydantic v1/v2 两代 API，当前环境下端点真实报错（如 agent limits 更新端点 500） | 对应端点功能不可用 | **建议单独立项**：统一 pydantic 兼容层后 22 个测试预计全绿 |
| B-2 | 环境声明族 | `requirements.txt` 缺 Pillow（computer_use 截图链路）、环境缺 `prometheus_client`（17 个 app 创建 ERROR） | CI 信号污染、computer_use 依赖未声明 | Pillow 入 requirements 一行修（打包是否含遵循"打包必须用户决定"约定）；prometheus_client 装包或降级可选依赖 |
| B-3 | N1 `sip.py` | `_init_production_mode`/`_send_production_audio`/`_init_dev_mode` 假成功残留（仅经 play_welcome_message 可达，当前零外部触发） | SIP 实装时会暴露 | 随 SIP 实装批次清理 |
| B-4 | N2 Gen1 `send_message` 签名错位 | discord/qqbot/qq/websocket 为 `(message: UnifiedMessage)`，与基类 `async send_message(chat_id, content, ...)` 不同契约 | manager 未来按基类签名直连会 TypeError | 统一签名或双方法（参照 qclaw 先例） |
| B-5 | N3 `agent/loops/base.py:200`、`post_chat_pipeline.py:616` | 只读 public `current_user_id` 别名（MagicMock 影子风险，与已修 #5 同源；生产语义正确） | 测试替身覆盖时才暴露 | 测试覆盖到时同步改读序 |
| B-6 | N4 mobile WS 单连接取消时序 | 同一 WS 连接的 `chat:cancel` 只能在流结束后被处理（#3 修复已使其无害）；中途取消需移动端双连接 | 移动端"停止"体验 | 结构性，随移动端迭代 |
| B-7 | N7 `scheduler._execute_task_wrapper` | 每 job 新建事件循环（预存设计，drain 依赖其存在） | 架构债 | 随调度器重构评估 |
| B-8 | `context_pool.mark_turn_seen`（:390-402） | 每轮 ack 仍全池 O(n) 扫（hash 索引只覆盖 hash 维度，turn 维度未建索引） | 池大后每轮请求线性劣化 | 仿 _by_hash 建 turn 索引，小修 |
| B-9 | 会话列表/feedback 统计 | list_sessions 摘要仍全量 json.load 所有会话文件（home 计数已修零解析，列表与点赞统计未动） | 历史大后列表/统计秒级延迟 | 摘要 sidecar 索引——**存储设计变更，需拍板** |
| B-10 | `neurova_recall._run_coroutine_in_thread` | 每轮记忆召回派生新线程+`asyncio.run` 新事件循环；M-13 超时后线程弃置不可取消（挂起场景渐进堆积） | 每轮 chat 的线程/循环 churn | 改常驻单事件循环线程，中改 |
| B-11 | 死代码接线前补界 | `tool_cache` L2/L3 无界、`anthropic_client._SyncStreamBridge` 每 chunk 新建事件循环、`auto_context_updater`（死代码，接线即周期 CPU 尖峰）、`task_tracker._max_tasks` 未执行、`subagent._spawned` 只增不删 | 当前零调用方，接线即升级 | 接线前必须先补界 |

### C. 测试债（预存失败甄别结论，非生产逻辑缺陷）

- tests/test_channels/test_unified_message.py×4、test_session_manager.py×14E：ad-hoc 目录，基线复现（与 production 代码无关；test_channels/test_session_manager 错误为 app 创建 prometheus 缺失链）。
- tests/unit/agent/（browser_tools×9 `BrowserCommandAdapter` 符号缺失、gate_catalog×6、tool_pipeline×4、llm_error_taxonomy×3、loop_manager×1、retrieval_progress×1）：基线 worktree 对照逐一致——预存，多为契约漂移的旧测试。
- pydantic 族 22 项计入 B-1（这些是真实端点 bug 的测试投影）。

> **⚠️ C 档终局甄别修正（2026-09-11 深夜，双环境对照后推翻部分早期归因）**：
> 上面的"契约漂移族"定性有误。系统 `python`（3.15.0a7）**缺 numpy 与 openai 包**——numpy 缺失 → 嵌入引擎降级 TF-IDF → 抽屉按相关性阈值过滤条目（这才是 context 抽屉序 4 例的真相，**会话序保持语义没坏**）；openai 缺失 → llm_client 条件导入符号缺失（error_taxonomy/loop_manager 3+1 例）+ retrieval_progress 的 numpy 导入（1 例）。**双环境对照（系统 vs `.venv`）逐例实测**：上述全部失败在 `.venv` 下为零，生产代码零 bug。
> 唯一真问题：`test_resolution_context_injection.py` 是**损坏测试**（async 无标记 + 被测 API 实为同步 + 测试体零断言异常全吞）——已按现行同步契约重写为真断言（4 前缀逐字段比对），双环境绿，净 −21 行，生产代码 0 改动。
> **结论固化：剩余 12 项中 11 项为系统解释器环境族（.venv 下全部为绿），1 项损坏测试已修。回归请统一走 `.venv`（3.12）；系统 python 仅建议用于轻量脚本。**
> 同根因放大排查：全仓 305 个含 `async def test_` 文件扫描，无标记候选逐一甄别无隐藏命中。新登记：neurflow `TestEvolutionNodeSignature`×2（并行会话 evolution 未提交改动的测试未跟上，留其收口）；`test_approval_reply_mechanism` 一例 .venv 下挂死（基线即挂，与近期改动零交集）。

### D. 前端非 UI 类

- 无新增（openImageTab 去重键 UX 已随 #18 修复；Html/Text/Md 面板审计干净）。

---

## 六、终账批次执行记录（2026-09-11 第四批，"开始全部修复"）

> 背景：用户拍板"按台账全部修复"。执行中发现**第三批并行会话**已将 B-1/B-2/B-3/B-4/B-5/B-6/B-7 立项/B-8/B-10/B-11 大部落盘（未提交）并更新本台账第五节状态头；本批四路代理按分工执行"核验 + 补缺 + 深化"：

| 项 | 第三批已落 | 第四批动作 |
|---|---|---|
| B-1/B-2 | 环境漂移根因（pydantic 1.10→2.13.5，零代码转绿 20/22）+ 装包 + **连带真 bug：MemoryManager.close() 空壳 WAL 连接泄漏 + agent_shutdown 持有人漏项（test_agent_package_api 16/16 转绿）** | A 路逐项实测核实；test_unified_message.py 4 例按现行 dataclass 契约适配（+5）；browser_tools 44/44 核实绿 |
| B-3/B-4/B-5/B-6 | sip 诚实化/Gen1 签名/身份读序/WS spawn 均已在位 | D 路逐条核验 + 约束对照，补齐 35 个锚测试（sip 10/b4 16/b5 4/中途取消 5），全仓放大视角零漏点 |
| B-7 | 单独立项：docs/B7调度器事件循环统一立项_2026-09-11.md（P1-P4 定性/三阶段方案/预估 2 天） | 主线采纳：per-job 循环属核心框架隔离设计，遵循"不影响核心框架"约束不强行改 |
| B-8 | `_by_turn` 索引已在位 | C 路逐点核验（六同步点全在位）+ 补 4 用例 |
| B-9 | v1：(路径,mtime,size) LRU 摘要缓存 + get_feedback_counts | **B 路升级 v2 sidecar 索引**：`_summary_index.json`（schema 版本 + fp=mtime_ns+size 指纹自愈）+ 10 处写路径接线 + list/feedback/stats 读路径切换 + 24 用例红→绿（零会话文件解析断言、8 线程并发、损坏自愈）；v1 LRU 降为指纹失配回退 |
| B-10 | 常驻单事件循环 | C 路发现真缺口：超时未真取消协程（共享 loop 上协程级泄漏）——`future.cancel()` 根修，红→绿 +3 行 |
| B-11 | tool_cache/auto_context_updater/_SyncStreamBridge 整体删除（优于补界，零调用方）+ task_tracker 死路径删除 + subagent 有界 | C/D 路核验 + 防复活锁存测试（tool_cache 2 例）+ task_tracker 存活方法覆盖重构（37 例） |
| C 档 | — | 主线删除陈旧 ad-hoc tests/test_channels/test_session_manager.py（所测 channels 旧版 SessionManager API 已死亡，现行实现由正式套件覆盖）；test_channels 目录 22 绿 |

**终态回归**：渠道 347 绿、api 1318 绿、agent 935 绿、core 全绿、前端 1306 绿 + vue-tsc 0；已知预存（tools/context 契约漂移族 70、round_ops 族 20、环境二进制族 6）经 worktree 基线差分逐条一致，零新增失败。**净 LOC**：第三批 ≈−1100 + 第四批（sidecar +585、B-10 +3、B-4 契约 +132、测试若干）。

**本批新登记**：①test_console_round_ops/test_console_chat_new 等 ad-hoc 用 1 参 lambda monkeypatch `_get_user_id`（两参签名漂移）——随 fixture 规约一并修；②sync ws_seq 2 例 WS 握手基线失败；③get_history/search_session 全量读文件有真实消费方（语义需全文，非索引化范围）；④TaskTracker 剩余 9 个不可达方法待机械清理（约 −150）；⑤建议回归统一走项目 `.venv`(3.12)——系统 python 3.15.0a7 的 lxml/PIL/greenlet 二进制族失败代码层不可修。

---

## 七、全量复核结论 + 前端契约匹配差距（2026-09-11 深夜复核轮）

> 复核范围：本日全部批次（19 项 + 台账 18 项 + 终账 B 档 11 项 + B-7 Phase1+2）。

**复核结论（闭环成立）**：
1. **静态断点清查**：已删符号（receive_message×5、tool_cache、auto_context_updater、_SyncStreamBridge、channels.mobile_pairing、task_tracker.start_tracking）全仓零悬空引用（剩余命中为注释/他类自有同名/防复活测试）；31 个修改文件 compileall + 导入冒烟全过。
2. **双解释器全域回归**：`.venv`(3.12 健康环境) **4601 passed / 0 failed / 18 skipped**——闭环零缺陷；系统 python 3.15.0a7 15 失败全部为已定性环境族（numpy/openai 缺失 + lxml/PIL/greenlet 二进制）。
3. **B-7**：G0/G1/G2 达成（选型 B、等价性逐帧全等、worker 占用不变量），scheduler 域 106 全绿，顺带根修 legacy drain 跨循环 gather 预存并发缺陷；Phase 3（48h 金丝雀/默认翻转/死配置清理）留生产化。

**前端契约匹配审查（只读审查 + vitest 1311 绿 + vue-tsc 0）**：本批后端改动**零 P1 断裂**——media 磁盘化/feedback stats 聚合/to_thread 三处/sidecar 索引/retry SSE（前端已有 case 'retry'）/sync 4401（前端已按码停连）/chat_busy（NeurUI 不消费 mobile WS，NeurUI 停止链路 console stop + task_tracker 闭环匹配）全部一致或可加性。发现 4 项**预存差距**（非本批引入）：

| # | 级别 | 定位 | 问题 |
|---|---|---|---|
| F-1 | P2 | `NeurUI/src/api/modules/media.ts` vs media.py | 系统性契约错位：listMedia 期望 `data.items`/page/size（后端 `data.media`/offset/limit）→ AgentMediaPage 列表恒空；getMediaInfo/downloadMedia/batchDeleteMedia/getMediaStats 四个端点路径全 404；id/tags/url 类型对不上 |
| F-2 | P2 | ChannelIntegrationPage.vue:610-620 + wechat.py:77,387-404 | wechat"测试连接"恒绿假阳性——恒发空 extra → authenticate 不执行 → connect() 无条件 True，不验证任何凭据 |
| F-3 | P2 | iLink 首次接入 UI | 无二维码展示/轮询组件（QR URL 仅后端日志）；无 token 时 save 请求 310s 超 axios 300s 超时窗（后端 to_thread 已不冻结全站，但 UI 断裂） |
| F-4 | P3 | chat.py:301-305 SSE audio 事件 | `audio_url` 为服务器本地路径非 HTTP URL（预存；当前主语音链路 autoVoice 不消费该字段，无用户可见影响） |

**建议**：F-1 是真实用户可见功能缺陷（媒体页恒空），优先排期；F-2/F-3 随 iLink 渠道完善批次（二维码轮询端点 + 前端扫码 UI 一并做）；F-4 登记不动。

> **✅ 四项全部修复（2026-09-11 第八批，三路并行代理）**：
> - **F-1 媒体契约对齐**：media.ts 四调用路径+类型全对齐后端真实契约（media_id/filename/metadata；list 消费 data.media/total/offset/limit；下载改 `/download/{id}` 字节流→objectURL）；后端新增 `POST /media/batch-delete`（succeeded/failed 回报）+ **路由顺序根修**（GET/PUT /config 原被 /{media_id} 吞成 404——与 getMediaStats 同根因的放大视角命中点，字面段路由移到参数路由前）；AgentMediaPage 下载/详情/缩略图真实可用（凭证据 Bearer 拉 blob + Map 缓存 + 卸载统一 revoke）；media.downloadError i18n ×11。红→绿：后端 7 例（3 红）+ 前端 11 例（10 红）。
> - **F-2 测试假阳性堵死**：前端 testChannel 改发已保存配置的真实 extra（根因=恒发 `{}`）；后端 test 端点 wechat 有 token → 真实验证（有界 10s）+ `_wechat_authenticated()` 核验初始化标志，verify 失败诚实 success:false；无 token → 诚实 needs_scan。
> - **F-3 扫码 UI 闭环**：后端拆非阻塞两段——`POST .../wechat/ilink/qrcode`（只生成零等待，返回 qr_url/qr_id；有有效 token 返回 ready）+ `GET .../qrcode/status`（单次轮询，confirmed 真实落盘 token）；save 无 token 跳过适配器注册并返回 needs_scan:true（消灭 300s 阻塞）；前端新增 WechatQrcodeDialog（二维码 + 等待/已扫描/已确认/已过期状态机 + 3s 轮询 + 过期重生成 + 关闭/卸载全路径清定时器），confirmed 后自动重发 save 完成注册闭环。i18n channel.* ×10 键 ×11 语言。红→绿：后端 27 例 + 前端 9 例。
> - **F-4 音频 URL 可服务化**：调查发现前端消费点真实存在（autoVoice 关 + enable_tts 开时 ChatPage audio/tts 分支与 done 兜底是活路径，本地路径必然播不了——台账"主链路不消费"定性修正）。根修：新增鉴权端点 `GET /api/v1/chat/tts-audio/{agent_id}/{filename}`（登录态+agent 访问权+tts_*.wav 白名单+包含性检查，无匿名暴露），chat.py 两个发射点（SSE audio 事件 + 非流式 data.audio.url + done audio_url）改输出 HTTP URL；前端 attachAudioUrl 带凭证 fetch→blob（失败静默回落手动合成）。顺带根修 chat.py 预存 dead import（pathlib.Path 被 fastapi.Path 遮蔽）。红→绿：7 例（本地路径外泄实证→取回同字节/401/403/穿越 400）。
> - **合并态验证**：后端 api+channels 组合 1725 passed / 2 failed（lxml 环境族）；前端 vitest 1331 passed / 0 failed + vue-tsc 0；i18n 键一致性（11 语言）通过。生产净 LOC：F-1 +114 / F-2+F-3 +547（WechatQrcodeDialog +248 为主）/ F-4 +70。
> - **新登记（相邻）**：①wechat.py connect() 仍恒 True——manager 启动链路不感知认证失败（test 端点已用 _wechat_authenticated() 兜住，根修涉及 manager 生命周期）；②服务启动/程序化路径的同步 300s 轮询仍在（save/test 端点已不触达）；③AgentMediaPage 搜索为前端过滤仅覆盖已加载页，后端无 search 参数；④media.py 三个死 Pydantic 模型未引用；⑤GET /media/stats 单段路径若未来要用需移到参数路由前。
