# Neurova 工具调用链升级计划（Needle 对比落地，不含 Needle 模型接入）

> **For agentic workers:** 本计划按 checkbox（`- [ ]`）逐任务执行；若执行会话装有 superpowers 系技能则用 subagent-driven-development / executing-plans 驱动，否则由主代理按 AGENTS.md 红绿灯 TDD 纪律逐任务实施（先写测试跑红，再实现跑绿，每任务一提交）。

**Goal:** 把 Needle 2 对比分析中确认可借鉴的 6 项"契约化"设计落到 Neurova 工具调用链：schema 一致性护栏、执行前参数校验、语义工具检索、肌肉记忆真语义置信、参数落地校验、工具面验收集。

**Architecture:** 全部落在既有单一咽喉点上——schema 事实源 `neurova/builtin_tools.py::_BUILTIN_SCHEMAS`、执行入口 `neurova/tool_executor.py::_execute_single_tool`（param_guard 之后、治理预检之前插校验）、检索模块 `neurova/context/tool_search.py`（已存在，只升级排序算法）、反射层 `neurova/cognitive_layers/memory_layer/muscle_memory.py`。不新增框架层，不改 ChatPipeline 结构。

**Tech Stack:** Python 3.10+、jsonschema 4.26（已在 .venv，需补进 requirements.txt）、现有 bge ONNX 本地嵌入引擎（`neurova.embedding.get_embedding_engine().encode(text) -> List[float]`）、pytest（tests/unit/ 既有结构）。

**来源与决策记录：**
- 来源：`github.com/cactus-compute/needle`（Needle 2，Apache-2.0）对比分析，2026-09-13。
- 决策：**不接入 Needle 模型本体**（45M 能力包线是"≤5 工具/闭集枚举/窄域微调"，中文能力未验证，不匹配 Neurova 62+ 异构工具与中文主场景）。本计划只移植其六个设计机制。
- 约束：遵守 [[neurova-incremental-only-workflow]]（只提升不下降）——每项新能力带独立 env 开关，关闭/引擎缺失时**逐字节回落现行为**。

---

## 现状证据锚点（勘察实录，实施前复核）

| 事实 | 锚点 |
|---|---|
| 62+ 内置工具 schema 全手写单源 | `neurova/builtin_tools.py:21`（`_BUILTIN_SCHEMAS`） |
| 执行分派表与 schema 已现漂移：`execute_code`/`search` 两键无 schema（系别名） | `neurova/tool_executor.py:236-303`（`_builtin_dispatch`），2026-09-13 AST 对账实测 |
| 执行前无通用类型/枚举/范围校验；ParamGuard 只修 JSON 不校验，默认未装配 | `neurova/tool_executor.py:1027-1039`、`neurova/security/tool_param_guard.py:118` |
| 参数解析失败的回传自纠通道（校验失败复用此错误形态） | `neurova/agent/loops/base.py:133-157` |
| Tool Search 已存在但为 BM25 词法、env 默认关 | `neurova/context/tool_search.py:92-118`、`neurova/context/orchestrator.py:1397-1407` |
| 本地嵌入引擎单例 | `neurova/embedding/__init__.py`（`get_embedding_engine`），`onnx_embedding.py:282`（`encode`） |
| 肌肉记忆"向量指纹"实为 MD5 前 16 位 | `neurova/cognitive_layers/memory_layer/muscle_memory.py:493-495` |
| 置信度权重：关键词 0.6/0.4、"向量" 0.3、成功率 0.1 | `muscle_memory.py:270-307` |
| 执行器可测性：`ToolExecutor(agent_ref)` 单参构造，测试已有先例 | `tool_executor.py:314`、`tests/unit/test_tool_execution_manager.py` |
| data 目录 JSON 持久化惯例 | `neurova/security/governance_settings.py:33`（`Path("data") / "*.json"`） |

## 文件结构地图（本计划触碰的全部文件）

```
neurova/security/tool_arg_validator.py     # 新建（T2/T5 共用 schema 查表+开关）
neurova/security/arg_grounding.py          # 新建（T5）
neurova/tool_executor.py                   # 修改：_execute_single_tool 插校验/落地两道闸（T2/T5）
neurova/agent/chat_pipeline.py             # 修改：设置本轮可见文本 ContextVar（T5，1 处）
neurova/context/tool_search.py             # 修改：BM25 → BM25+嵌入混合（T3）
neurova/cognitive_layers/memory_layer/muscle_memory.py  # 修改：语义置信（T4）
requirements.txt                           # 修改：jsonschema>=4.0（T2）
tests/unit/tools/test_tool_schema_contract.py           # 新建（T1）
tests/unit/security/test_tool_arg_validator.py          # 新建（T2）
tests/unit/context/test_tool_search_semantic.py         # 新建（T3）
tests/unit/memory/test_muscle_memory_semantic.py        # 新建（T4）
tests/unit/security/test_arg_grounding.py               # 新建（T5）
tests/unit/tools/test_tool_surface_acceptance.py        # 新建（T6）
tests/unit/tools/fixtures/tool_surfaces/*.json          # 新建（T6）
```

## env 开关总表（回滚 = 设 0 / 删 env，零残留）

| 变量 | 默认 | 关/缺时行为 |
|---|---|---|
| `NEUROVA_TOOL_ARG_VALIDATION` | 1（开） | =0 时 T2/T5 校验完全不执行，逐字节现行为 |
| `NEUROVA_TOOL_SEARCH_EMBEDDING` | 1（开） | =0 或引擎不可用时回落纯 BM25（现算法） |
| `NEUROVA_MUSCLE_SEMANTIC` | 1（开） | =0 或条目无嵌入向量时回落 MD5 等值语义 |
| `NEUROVA_ARG_GROUNDING` | **0（关）** | 灰度期显式设 1；默认关零影响 |

---

## Task T1: Schema 契约护栏（纯测试，先红后绿）

**目的：** 把"手写 schema ↔ 执行函数"从隐性约定变成机器钉死的回归契约。方向 A：dispatch/schema 键对账（防"执行器能跑但模型看不见"/"模型能传但无人接"）；方向 B：AST 提取 `_execute_*` 方法读取的参数名 ⊆ schema 声明（防参数被静默丢弃——ChannelIntegrationPage 同族根因）；方向 C：每个 parameters 都是合法 JSON Schema。

**Files:**
- Create: `tests/unit/tools/test_tool_schema_contract.py`（`tests/unit/tools/` 已存在）
- Modify: 红→绿过程中按违规实据修 `neurova/builtin_tools.py` 或 `neurova/tool_executor.py`

- [ ] **Step 1: 写失败测试**

```python
"""T1 Schema 契约护栏 — docs/Neurova_工具调用链升级计划_2026-09-13.md。

钉三件事：dispatch/schema 键对账、执行体读取参数 ⊆ schema 声明、
_BUILTIN_SCHEMAS 全部为合法 JSON Schema（Draft 2020-12）。
"""
import ast
import re
from pathlib import Path

import pytest
from jsonschema import Draft202012Validator

from neurova.builtin_tools import _BUILTIN_SCHEMAS
import neurova.tool_executor as _te

_SRC = Path(_te.__file__).read_text(encoding="utf-8")

# dispatch 表中不另立 schema 的既有别名（2026-09-13 对账实据）；
# 修复时若判定应补 schema 则删条目、改 builtin_tools
DISPATCH_ALIASES = {"execute_code": "run_code", "search": "web_search"}

# to_openai_format 统一注入的展示参数（builtin_tools.py:870-879），执行器读取合法
_INJECTED_KEYS = {"taskNameActive", "taskNameComplete"}

# 红灯期逐条裁决后的豁免表：工具名 → 执行体合法读取但未进 schema 的键
# （例：内部透传/上游注入）。每条必须带行内理由，无登记的违规一律算 bug。
READ_EXEMPT: dict[str, set] = {}


def _dispatch_map() -> dict:
    m = re.search(r"_builtin_dispatch\s*=\s*\{(.*?)\n    \}", _SRC, re.S)
    assert m, "_builtin_dispatch 字面量结构变化，同步本测试解析器"
    return dict(re.findall(r'"([a-z_0-9.]+)":\s*"(_execute_\w+)"', m.group(1)))


def _method_ast(name: str):
    tree = ast.parse(_SRC)
    cls = next(n for n in tree.body if isinstance(n, ast.ClassDef) and n.name == "ToolExecutor")
    fn = next((f for f in cls.body if isinstance(f, ast.FunctionDef) and f.name == name), None)
    assert fn, f"ToolExecutor 缺失 {name}"
    return fn


def _params_read(fn) -> set:
    keys = set()
    for node in ast.walk(fn):
        if isinstance(node, ast.Call) and isinstance(node.func, ast.Attribute) \
                and node.func.attr == "get" and isinstance(node.func.value, ast.Name) \
                and node.func.value.id == "params" and node.args \
                and isinstance(node.args[0], ast.Constant) and isinstance(node.args[0].value, str):
            keys.add(node.args[0].value)
        if isinstance(node, ast.Subscript) and isinstance(node.value, ast.Name) \
                and node.value.id == "params" and isinstance(node.slice, ast.Constant):
            keys.add(node.slice.value)
    return keys


def test_dispatch_schema_parity():
    extra = set(_dispatch_map()) - set(_BUILTIN_SCHEMAS) - set(DISPATCH_ALIASES)
    assert not extra, f"dispatch 存在未登记 schema 的工具: {sorted(extra)}"


def test_all_builtin_schemas_valid_json_schema():
    bad = []
    for name, entry in _BUILTIN_SCHEMAS.items():
        try:
            Draft202012Validator.check_schema(entry["parameters"])
        except Exception as exc:
            bad.append(f"{name}: {exc.message}")
    assert not bad, "非法 parameters schema:\n" + "\n".join(bad)


@pytest.mark.parametrize("tool", sorted(_BUILTIN_SCHEMAS))
def test_executor_reads_only_declared_params(tool):
    method = _dispatch_map().get(tool)
    if method is None:
        pytest.skip("非 builtin dispatch 工具")
    declared = set((_BUILTIN_SCHEMAS[tool]["parameters"] or {}).get("properties") or {})
    undeclared = _params_read(_method_ast(method)) - declared - _INJECTED_KEYS - READ_EXEMPT.get(tool, set())
    assert not undeclared, f"{tool} 执行体读取未声明参数（模型无法供给）: {sorted(undeclared)}"
```

- [ ] **Step 2: 跑红**

Run: `pytest tests/unit/tools/test_tool_schema_contract.py -v`
Expected: parity 测（已移除两个别名的登记前）红；`test_executor_reads_only_declared_params` 若干参数化红（静默丢参/隐藏读参实据）。若全绿说明勘察有漏，回读 `_SRC` 锚点重对账。

- [ ] **Step 3: 逐违规裁决（三选一，不许加守卫消音）**

对每个红灯工具名：
1. 执行体读了 schema 没有的键 → **补 schema 声明**（该参数本应暴露给模型）；
2. 键是上游注入/内部字段 → 登记进 `READ_EXEMPT[tool] = {"key"}` 并**行内注释理由**；
3. 执行体读错键名（真 bug，参数永远拿不到）→ 修执行函数取参名，补一条复现用例。

- [ ] **Step 4: 跑绿 + 回归**

Run: `pytest tests/unit/tools/test_tool_schema_contract.py tests/unit/test_tool_bugs_v2.py tests/unit/test_tool_bugs_v3.py -q`
Expected: 全绿（后两文件是执行器既有回归面）。

- [ ] **Step 5: 提交**

```bash
git add tests/unit/tools/test_tool_schema_contract.py neurova/
git commit -m "test(tools): T1 schema契约护栏 — dispatch/schema对账+AST读取参数⊆声明+JSON Schema合法性，红灯期逐条裁决静默丢参"
```

---

## Task T2: 执行前参数校验（约束解码的后置等价）

**目的：** 云端模型无法文法约束，等价做法：执行前按 schema 校验，失败不执行、逐字段错误回传（走 `base.py:133` 同款错误通道让 LLM 自纠）。校验前先做宽容标量归一（`"27"→27`），未知键 jsonschema 默认放行（additionalProperties 缺省 true，天然宽容）。

**Files:**
- Create: `neurova/security/tool_arg_validator.py`
- Create: `tests/unit/security/test_tool_arg_validator.py`
- Modify: `neurova/tool_executor.py`（param_guard 块之后、MCP grant 检查之前，约 :1040）
- Modify: `requirements.txt`（追加 `jsonschema>=4.0`；4.26.0 已在 .venv）

- [ ] **Step 1: 写失败测试**

```python
"""T2 执行前参数校验单测。"""
import os
import pytest

from neurova.security.tool_arg_validator import validation_enabled, validate_tool_args


@pytest.fixture(autouse=True)
def _env_on(monkeypatch):
    monkeypatch.delenv("NEUROVA_TOOL_ARG_VALIDATION", raising=False)


def test_disabled_env_returns_passthrough(monkeypatch):
    monkeypatch.setenv("NEUROVA_TOOL_ARG_VALIDATION", "0")
    assert not validation_enabled()
    params, errors = validate_tool_args("weather", {"city": {"not": "a string"}})
    assert errors == [] and params == {"city": {"not": "a string"}}


def test_unknown_tool_no_schema_passes():
    params, errors = validate_tool_args("no_such_tool_xyz", {"anything": 1})
    assert errors == []


def test_string_number_coerced_then_passes():
    # weather 的 city 为 string；对 integer/number 字段测宽容归一：
    # recall_history.limit 是 integer，模型常以 "5" 字符串回传
    params, errors = validate_tool_args("recall_history", {"query": "x", "limit": "5"})
    assert errors == [] and params["limit"] == 5


def test_type_mismatch_rejected_with_field_path():
    params, errors = validate_tool_args("recall_history", {"query": "x", "limit": {"bad": 1}})
    assert errors and errors[0].startswith("limit:")


def test_missing_required_rejected():
    params, errors = validate_tool_args("memory_search", {"limit": 3})
    assert any("query" in e or "required" in e.lower() for e in errors)


def test_unknown_key_not_fatal():
    params, errors = validate_tool_args("recall_history", {"query": "x", "typo_key": 1})
    assert errors == []
```

- [ ] **Step 2: 跑红**

Run: `pytest tests/unit/security/test_tool_arg_validator.py -q`
Expected: FAIL — `ModuleNotFoundError: neurova.security.tool_arg_validator`。

- [ ] **Step 3: 实现校验模块**

```python
# neurova/security/tool_arg_validator.py
"""工具参数执行前校验（工具调用链升级计划 T2/T5 共用的 schema 查表与开关单源）。

Needle 对比结论：云端模型不可文法约束，后置等价=执行前 jsonschema 校验，
失败拒执行、逐字段错误经 role=tool 通道回传 LLM 自纠（与 loops/base.py
解析失败同通道）。宽容原则：标量字符串化先归一；未知键放行（存量工具
普遍多收参数，防误伤）。NEUROVA_TOOL_ARG_VALIDATION=0 全局关闭。
"""
import os
from typing import Any, Dict, List, Optional, Tuple

from jsonschema import Draft202012Validator

from neurova.core.logger import get_logger

logger = get_logger(__name__)

_MAX_ERRORS = 8


def validation_enabled() -> bool:
    return os.environ.get("NEUROVA_TOOL_ARG_VALIDATION", "1") != "0"


def get_tool_arg_schema(tool_name: str) -> Optional[Dict[str, Any]]:
    """内置工具 parameters schema（单源 builtin_tools 注册表）；查无返回 None。"""
    try:
        from neurova.builtin_tools import get_builtin_tool_params

        entry = get_builtin_tool_params(tool_name)
    except Exception:  # noqa: BLE001 - 查表失败降级放行，不阻断执行
        return None
    if not isinstance(entry, dict):
        return None
    schema = entry.get("parameters")
    return schema if isinstance(schema, dict) else None


def _coerce(value: Any, typ: Optional[str]) -> Any:
    if not isinstance(value, str) or not typ:
        return value
    text = value.strip()
    try:
        if typ == "integer":
            return int(text)
        if typ == "number":
            return float(text)
        if typ == "boolean" and text.lower() in ("true", "false"):
            return text.lower() == "true"
    except ValueError:
        return value
    return value


def validate_tool_args(tool_name: str, params: Dict[str, Any]) -> Tuple[Dict[str, Any], List[str]]:
    """返回 (归一后参数, 逐字段错误列表)。错误列表非空即应拒绝执行。"""
    schema = get_tool_arg_schema(tool_name)
    if schema is None:
        return params, []
    props = schema.get("properties") or {}
    out = dict(params or {})
    for key, spec in props.items():
        if key in out and isinstance(spec, dict):
            out[key] = _coerce(out[key], spec.get("type"))
    errors: List[str] = []
    for err in sorted(Draft202012Validator(schema).iter_errors(out), key=lambda e: list(e.path)):
        path = ".".join(str(p) for p in err.path)
        errors.append(f"{path or '(root)'}: {err.message}")
        if len(errors) >= _MAX_ERRORS:
            break
    return out, errors
```

- [ ] **Step 4: 跑绿（模块）**

Run: `pytest tests/unit/security/test_tool_arg_validator.py -q`
Expected: 7 用例全绿。

- [ ] **Step 5: 写接线失败测试**

```python
# 追加到 tests/unit/security/test_tool_arg_validator.py
import asyncio
from unittest.mock import MagicMock

from neurova.tool_executor import ToolExecutor


def test_executor_rejects_bad_args_before_core_execution(monkeypatch):
    monkeypatch.delenv("NEUROVA_TOOL_ARG_VALIDATION", raising=False)
    ex = ToolExecutor(MagicMock())
    reached = {"core": False}

    async def _spy_core(tool_name, params):
        reached["core"] = True
        return ({"ok": True}, True, "spy")

    monkeypatch.setattr(ex, "_execute_tool_core", _spy_core)
    result = asyncio.run(ex._execute_single_tool("recall_history", {"query": "x", "limit": {"bad": 1}}))
    assert not reached["core"], "校验失败仍打到了执行核心"
    assert result["success"] is False and any("limit" in e for e in result["param_errors"])


def test_executor_env_off_skips_validation(monkeypatch):
    monkeypatch.setenv("NEUROVA_TOOL_ARG_VALIDATION", "0")
    ex = ToolExecutor(MagicMock())
    called = {"core": False}

    async def _core(tool_name, params):
        called["core"] = True
        return ({"ok": True}, True, "spy")

    monkeypatch.setattr(ex, "_execute_tool_core", _core)
    asyncio.run(ex._execute_single_tool("recall_history", {"query": "x", "limit": {"bad": 1}}))
    assert called["core"]
```

Run: `pytest tests/unit/security/test_tool_arg_validator.py -q` → 新增两测 FAIL（未接线）。
若 finally 钩子对 mock agent 有额外属性要求，按报错在 `MagicMock()` 上补齐，不改生产代码。

- [ ] **Step 6: 接线 `_execute_single_tool`**

在 `neurova/tool_executor.py` param_guard 块结束（:1039 `return result` 之后）与 `# 方案 P0-1.5: 统一治理预检` 注释之间插入：

```python
            # 工具链升级 T2：schema 约束的后置等价——执行前参数校验，失败拒执行、
            # 逐字段错误回传 LLM 自纠（与 loops/base.py 解析失败同错误通道）。
            from neurova.security.tool_arg_validator import validation_enabled, validate_tool_args

            if validation_enabled():
                params, _arg_errors = validate_tool_args(tool_name, params)
                if _arg_errors:
                    result = {
                        "success": False,
                        "error": "参数校验未通过: " + "; ".join(_arg_errors),
                        "param_errors": _arg_errors,
                    }
                    return result
```

- [ ] **Step 7: 跑绿 + 回归 + 补依赖**

```bash
pytest tests/unit/security/test_tool_arg_validator.py tests/unit/test_tool_execution_manager.py tests/unit/tools/test_tool_schema_contract.py -q
echo "jsonschema>=4.0" >> requirements.txt
```
Expected: 全绿。再跑 `pytest tests/unit/tools tests/unit/security tests/unit/agent -q` 确认无既有工具链测试因新校验翻红——如有，逐个判定是"该测试此前喂的就是非法参数"（红测改对）还是误伤（校验收窄），不许 skip。

- [ ] **Step 8: 提交**

```bash
git add neurova/security/tool_arg_validator.py neurova/tool_executor.py tests/unit/security/test_tool_arg_validator.py requirements.txt
git commit -m "feat(tools): T2 执行前参数校验 — jsonschema后置等价文法约束，宽容归一+逐字段错误回传自纠，env可关"
```

---

## Task T3: Tool Search 语义化（BM25 → BM25+bge 混合，指纹增量索引）

**目的：** `search_catalog` 保持签名与 BM25 兜底不变，叠加本地 bge ONNX 嵌入余弦做混合排序；嵌入向量按 Needle 式"schema+模型指纹"持久化，schema 变更只重嵌变化条目。**不改** A6 的激活门（`NEUROVA_TOOL_SEARCH` 默认关的现状维持，是否默认开另行拍板）。

**Files:**
- Modify: `neurova/context/tool_search.py`
- Create: `tests/unit/context/test_tool_search_semantic.py`
- 持久化: `data/tool_search_index.json`（运行时生成，gitignore 已覆盖 data/ 则无需动作；否则补 ignore 条目）

- [ ] **Step 1: 写失败测试**

```python
"""T3 语义混合检索：无引擎=纯BM25零回归；有引擎=语义近但词面不交的条目上位；
指纹一致复用索引不重嵌；指纹变化只重嵌变化条目。"""
import json
import math
from pathlib import Path

import pytest

from neurova.context import tool_search as ts


class FakeEngine:
    """字符 one-hot 的确定性假引擎（中文语境够用）。"""

    def __init__(self):
        self.encode_calls: list[str] = []

    @property
    def dimension(self):
        return 512

    def encode(self, text: str):
        self.encode_calls.append(text)
        vec = [0.0] * 512
        for ch in set(text):
            vec[ord(ch) % 512] += 1.0
        norm = math.sqrt(sum(v * v for v in vec)) or 1.0
        return [v / norm for v in vec]


@pytest.fixture
def entries():
    return ts.build_catalog([
        {"type": "function", "function": {"name": "weather_lookup",
          "description": "查询指定城市的实时天气与温度",
          "parameters": {"type": "object", "properties": {"city": {"type": "string"}}}}},
        {"type": "function", "function": {"name": "file_write",
          "description": "写入文件内容到指定路径",
          "parameters": {"type": "object", "properties": {"file_path": {"type": "string"}}}}},
    ])


@pytest.fixture
def isolated_index(tmp_path, monkeypatch):
    monkeypatch.setattr(ts, "_INDEX_PATH", str(tmp_path / "idx.json"))


def test_no_engine_falls_back_to_bm25_unchanged(entries, isolated_index, monkeypatch):
    # 查询用 "weather"（词法 token 命中 name）；中文描述在本 BM25 分词下是整段
    # 单 token，不能用"天气"二字测词法回落（那是 T3 语义要解决的问题本身）
    monkeypatch.setattr(ts, "_get_engine", lambda: None)
    hits = ts.search_catalog("weather", entries, limit=2)
    assert hits and hits[0]["name"] == "weather_lookup"


def test_semantic_lifts_keyword_disjoint_match(entries, isolated_index, monkeypatch):
    eng = FakeEngine()
    monkeypatch.setattr(ts, "_get_engine", lambda: eng)
    # 词面刻意不交："气温" 与 BM25 token 无交集，仅字符嵌入有重叠
    hits = ts.search_catalog("气温如何", entries, limit=1)
    assert hits[0]["name"] == "weather_lookup"


def test_fingerprint_reuse_skips_reembed(entries, isolated_index, monkeypatch):
    eng = FakeEngine()
    monkeypatch.setattr(ts, "_get_engine", lambda: eng)
    ts.search_catalog("气温如何", entries, limit=1)          # 首跑：查询1 + 目录2
    first = len(eng.encode_calls)
    assert first == 3
    ts.search_catalog("气温如何", entries, limit=1)          # 二跑：仅重嵌查询
    assert len(eng.encode_calls) - first == 1, "目录向量应命中持久索引"


def test_schema_change_reembeds_only_changed(entries, isolated_index, monkeypatch):
    eng = FakeEngine()
    monkeypatch.setattr(ts, "_get_engine", lambda: eng)
    ts.search_catalog("weather", entries, limit=1)
    base = len(eng.encode_calls)
    entries2 = json.loads(json.dumps(entries))
    entries2[0]["description"] = "查询天气预报（含空气质量）"  # 仅改 weather 条目
    ts.search_catalog("weather", entries2, limit=1)
    delta = len(eng.encode_calls) - base
    assert delta <= 2, f"只应重嵌变化条目+查询，实际 {delta} 次"
```

- [ ] **Step 2: 跑红**

Run: `pytest tests/unit/context/test_tool_search_semantic.py -q`
Expected: FAIL — `_INDEX_PATH`/`_get_engine` 不存在、`os` monkeypatch 断言 AttributeError。

- [ ] **Step 3: 实现混合检索**

在 `neurova/context/tool_search.py` 追加（现 BM25 函数体重命名为 `_bm25_scores(query, entries) -> dict[str, float]`，`search_catalog` 保留原签名）：

```python
# ── T3 语义混合检索（docs/Neurova_工具调用链升级计划_2026-09-13）────────────
# NEUROVA_TOOL_SEARCH_EMBEDDING=0 或引擎缺失 → 纯 BM25（现行为逐字节不变）。
# 目录向量按（模型签名+维度）指纹持久化 data/tool_search_index.json，
# 条目 description 变更只重嵌该条（Needle tool_index 指纹增量式设计移植）。
import hashlib
import json
import threading

_EMB_WEIGHT = 0.6
_BM25_WEIGHT = 0.4
_INDEX_PATH = os.path.join("data", "tool_search_index.json")
_index_lock = threading.RLock()


def _embedding_enabled() -> bool:
    return os.environ.get("NEUROVA_TOOL_SEARCH_EMBEDDING", "1") != "0"


def _get_engine():
    try:
        from neurova.embedding import get_embedding_engine

        return get_embedding_engine()
    except Exception:  # noqa: BLE001 - 引擎缺失属正常态（未装模型）
        return None


def _embed_text(e: Dict[str, Any]) -> str:
    return f"{e['name']} {e['description']} {e.get('params_text', '')}"


def _entry_fingerprint(e: Dict[str, Any]) -> str:
    """条目内容指纹：description/参数名变更即触发该条重嵌（Needle 增量式）。"""
    return hashlib.sha256(_embed_text(e).encode("utf-8")).hexdigest()[:16]


def _model_fingerprint(engine) -> str:
    raw = f"{engine.__class__.__name__}:{getattr(engine, 'dimension', 0)}"
    return hashlib.sha256(raw.encode()).hexdigest()[:16]


def _load_index(fp: str) -> Dict[str, Dict[str, Any]]:
    """vectors 结构: {name: {"fp": 条目指纹, "v": 向量}}；模型级指纹不符整体作废。"""
    try:
        with open(_INDEX_PATH, "r", encoding="utf-8") as fh:
            doc = json.load(fh)
        if doc.get("fp") == fp:
            return doc.get("vectors") or {}
    except (OSError, ValueError):
        pass
    return {}


def _save_index(fp: str, vectors: Dict[str, Any]) -> None:
    os.makedirs(os.path.dirname(_INDEX_PATH) or ".", exist_ok=True)
    tmp = _INDEX_PATH + ".tmp"
    with open(tmp, "w", encoding="utf-8") as fh:
        json.dump({"fp": fp, "vectors": vectors}, fh, ensure_ascii=False)
    os.replace(tmp, _INDEX_PATH)


def _cosine(a, b) -> float:
    dot = sum(x * y for x, y in zip(a, b))
    na = math.sqrt(sum(x * x for x in a)) or 1.0
    nb = math.sqrt(sum(x * x for x in b)) or 1.0
    return dot / (na * nb)


def _hybrid_scores(query: str, entries: List[Dict[str, Any]]) -> Optional[Dict[str, float]]:
    """BM25 与嵌入余弦各按最高值归一后加权合并；引擎不可用返回 None（调用方回落 BM25）。"""
    if not _embedding_enabled():
        return None
    engine = _get_engine()
    if engine is None:
        return None
    bm25 = _bm25_scores(query, entries)
    try:
        q_vec = engine.encode(query)
    except Exception as err:  # noqa: BLE001 - 编码故障降级，不阻断检索
        logger.debug("T3 查询嵌入失败，回落 BM25: %s", err)
        return None
    with _index_lock:
        vectors = _load_index(_model_fingerprint(engine))
        changed = False
        for e in entries:
            stored = vectors.get(e["name"])
            if stored is None or stored.get("fp") != _entry_fingerprint(e):
                try:
                    vectors[e["name"]] = {"fp": _entry_fingerprint(e), "v": engine.encode(_embed_text(e))}
                    changed = True
                except Exception as err:  # noqa: BLE001 - 单条失败该条仅 BM25 分
                    logger.debug("T3 条目 %s 嵌入失败: %s", e["name"], err)
        if changed:
            _save_index(_model_fingerprint(engine), vectors)
    bmax = max(bm25.values()) if bm25 else 1.0
    scores: Dict[str, float] = {}
    for e in entries:
        stored = vectors.get(e["name"])
        sim = _cosine(q_vec, stored["v"]) if stored else 0.0
        combined = _EMB_WEIGHT * sim + _BM25_WEIGHT * (bm25.get(e["name"], 0.0) / (bmax or 1.0))
        if combined > 0:
            scores[e["name"]] = combined
    return scores


def search_catalog(query: str, entries: List[Dict[str, Any]], limit: int = 8) -> List[Dict[str, Any]]:
    """混合检索：语义+词法；引擎缺失/关闭时逐字节等价原 BM25 行为。"""
    if not query or not entries:
        return []
    scores = _hybrid_scores(query, entries)
    if scores is None:
        bm25 = _bm25_scores(query, entries)
        ranked = sorted(entries, key=lambda e: -bm25.get(e["name"], 0.0))
        return [e for e in ranked[: max(1, limit)] if bm25.get(e["name"], 0.0) > 0]
    ranked = sorted(entries, key=lambda e: -scores.get(e["name"], 0.0))
    return [e for e in ranked[: max(1, limit)] if scores.get(e["name"], 0.0) > 0]
```

（原 `search_catalog` 函数体中 BM25 计算段抽出为 `_bm25_scores`：对每个 entry 返回 `{name: score}`，词法逻辑一行不改。）

- [ ] **Step 4: 跑绿 + 回归**

Run: `pytest tests/unit/context/ -q`
Expected: 新 4 测绿 + 既有 tool_search 测试全绿（词法行为不变即证明）。

- [ ] **Step 5: 真引擎冒烟（live-verify）**

Run: `.venv/Scripts/python.exe -c "from neurova.context import tool_search as ts; from neurova.embedding import get_embedding_engine; assert get_embedding_engine() is not None; e=ts.build_catalog([{'type':'function','function':{'name':'weather','description':'查询城市实时天气','parameters':{'type':'object','properties':{'city':{'type':'string'}}}}}]); print([x['name'] for x in ts.search_catalog('气温多少度', e, 1)])"`
Expected: `['weather']`（真 bge 模型语义命中）。

- [ ] **Step 6: 提交**

```bash
git add neurova/context/tool_search.py tests/unit/context/test_tool_search_semantic.py
git commit -m "feat(context): T3 ToolSearch语义化 — bge嵌入×BM25混合排序+schema指纹增量索引(data/tool_search_index.json)，无引擎逐字节回落BM25"
```

---

## Task T4: 肌肉记忆语义置信（废 MD5 假向量的优先路径）

**目的：** `_compute_confidence` 的 0.3 向量分量从"MD5 等值"升级为"真嵌入余弦映射"；嵌入存进 `item.metadata["query_embedding"]`（不迁移 dataclass/落盘 schema，向后兼容）。引擎缺失、开关关闭、旧条目无向量三种情况**逐字节回落现行为**（只提升不下降）。

**Files:**
- Modify: `neurova/cognitive_layers/memory_layer/muscle_memory.py`（`match` :135、`match_by_query` :181、`_compute_confidence` :270、`record_usage` :338 附近、新增 3 个模块级小函数）
- Create: `tests/unit/memory/test_muscle_memory_semantic.py`

- [ ] **Step 1: 写失败测试**

```python
"""T4 语义置信：向量分量按余弦分档；条目无向量/开关关逐位回落 MD5 等值语义。"""
import math

import pytest

from neurova.cognitive_layers.memory_layer.muscle_memory import (
    MuscleMemory, MuscleMemoryItem, MemoryLevel,
)


class FakeEngine:
    """字符散列归一化假引擎（8 维，确定性）。"""

    dimension = 8

    def encode(self, text: str):
        v = [0.0] * 8
        for ch in set(text):
            v[ord(ch) % 8] += 1.0
        n = math.sqrt(sum(x * x for x in v)) or 1.0
        return [x / n for x in v]


@pytest.fixture(autouse=True)
def _engine_on(monkeypatch):
    monkeypatch.setenv("NEUROVA_MUSCLE_SEMANTIC", "1")
    import neurova.cognitive_layers.memory_layer.muscle_memory as mm
    monkeypatch.setattr(mm, "_get_engine", lambda: FakeEngine())


def _item(**kw) -> MuscleMemoryItem:
    base = dict(id="x1", tool_name="weather", query_fingerprint="北京,天气",
                vector_fingerprint="md5fp", level=MemoryLevel.L3)
    base.update(kw)
    return MuscleMemoryItem(**base)


def test_record_stores_semantic_vector():
    mem = MuscleMemory()
    item = mem.record_usage("weather", "查询北京的天气", {"city": "北京"}, True)
    assert item.metadata.get("query_embedding")


def test_high_cosine_gets_vector_credit():
    mem = MuscleMemory()
    item = _item(metadata={"query_embedding": [1.0] + [0.0] * 7})
    conf = mem._compute_confidence(item, "北京,天气", "other-md5",
                                   query_emb=[0.99, 0.14] + [0.0] * 6)
    # cos≈0.999 → (0.999-0.75)/0.2 封顶 1.0 → +0.3；关键词全等 +0.6
    assert conf == pytest.approx(0.9)


def test_low_cosine_falls_back_to_md5_equality():
    mem = MuscleMemory()
    item = _item(metadata={"query_embedding": [1.0] + [0.0] * 7})
    conf = mem._compute_confidence(item, "北京,天气", "md5fp",
                                   query_emb=[0.0, 1.0] + [0.0] * 6)
    # cos=0 ≤0.75 → 走 MD5 等值分支（vector_fp 相等 +0.3）
    assert conf == pytest.approx(0.9)


def test_low_cosine_wrong_md5_gets_nothing():
    mem = MuscleMemory()
    item = _item(metadata={"query_embedding": [1.0] + [0.0] * 7})
    conf = mem._compute_confidence(item, "北京,天气", "wrong-fp",
                                   query_emb=[0.0, 1.0] + [0.0] * 6)
    assert conf == pytest.approx(0.6)  # 语义两分支皆不加分


def test_switch_off_is_bitwise_identical(monkeypatch):
    monkeypatch.setenv("NEUROVA_MUSCLE_SEMANTIC", "0")
    mem = MuscleMemory()
    item = _item(metadata={"query_embedding": [1.0] + [0.0] * 7})
    emb = [0.99, 0.14] + [0.0] * 6
    assert mem._compute_confidence(item, "北京,天气", "md5fp", query_emb=emb) == \
        mem._compute_confidence(item, "北京,天气", "md5fp")


def test_record_then_match_full_confidence():
    mem = MuscleMemory()
    mem.record_usage("weather", "北京天气", {"city": "北京"}, True)
    hits = mem.match("weather", "北京天气", top_k=1)
    assert hits and hits[0][1] >= 0.9  # 全等 fp + 语义满档 + 成功率
```

- [ ] **Step 2: 跑红**

Run: `pytest tests/unit/memory/test_muscle_memory_semantic.py -q`
Expected: FAIL — `_get_engine` 属性不存在、`_compute_confidence` 无 `query_emb` 参数、`query_embedding` 不入库。

- [ ] **Step 3: 实现**

`muscle_memory.py` 顶部追加模块函数：

```python
import os as _os
import math as _math


def _semantic_enabled() -> bool:
    return _os.environ.get("NEUROVA_MUSCLE_SEMANTIC", "1") != "0"


def _get_engine():
    try:
        from neurova.embedding import get_embedding_engine

        return get_embedding_engine()
    except Exception:  # noqa: BLE001 - 未装模型属正常态
        return None


def _semantic_vector(text: str):
    if not text or not _semantic_enabled():
        return None
    engine = _get_engine()
    if engine is None:
        return None
    try:
        vec = engine.encode(text)
        # 存储瘦身：4 位小数足够分档，向量落盘体积减半以上
        return [round(float(x), 4) for x in vec]
    except Exception:  # noqa: BLE001
        return None


def _cosine(a, b) -> float:
    dot = sum(x * y for x, y in zip(a, b))
    na = _math.sqrt(sum(x * x for x in a)) or 1.0
    nb = _math.sqrt(sum(x * x for x in b)) or 1.0
    return dot / (na * nb)
```

`record_usage` 在 :338-339（`fingerprint`/`vector_fp` 两行）之后、`with self._lock:` 之前插入（此处 `metadata` 已由 :333-336 兜底为 dict）：

```python
        # T4：语义向量随条目入库（旧条目无该键 → _compute_confidence 逐位回落，
        # 见单测 test_record_then_match_full_confidence/test_switch_off_is_bitwise_identical）
        emb = _semantic_vector(query)
        if emb:
            metadata["query_embedding"] = emb
```

`_compute_confidence` 改签名与向量分量段（keyword/success 段一字不动）：

```python
    def _compute_confidence(
        self,
        item: MuscleMemoryItem,
        fingerprint: str,
        vector_fp: str,
        query_emb: Optional[List[float]] = None,
    ) -> float:
        ...
        # T4 向量分量：条目存有语义向量时，用余弦映射（0.75→0 线性到 0.95→满）
        # 替换 MD5 等值；任一侧缺失则逐位回落原 MD5 分支（只提升不下降）
        item_emb = item.metadata.get("query_embedding") if _semantic_enabled() else None
        if query_emb and item_emb:
            sim = _cosine(query_emb, item_emb)
            if sim > 0.75:
                score += 0.3 * min(1.0, (sim - 0.75) / 0.2)
            else:
                score += 0.3 if (vector_fp and item.vector_fingerprint == vector_fp) else 0.0
        elif vector_fp and item.vector_fingerprint == vector_fp:
            score += 0.3
        ...
```

`match`（:152-153 算完 fp 后）与 `match_by_query`（:198-199 同位置）各加一行 `query_emb = _semantic_vector(query)`；然后 `grep -n "_compute_confidence(" neurova/cognitive_layers/memory_layer/muscle_memory.py` 找出全部调用点，统一改为 `self._compute_confidence(item, fingerprint, vector_fp, query_emb=query_emb)`（`query_emb` 缺省 None，`tool_memory_integration` 等外部若有直调按位置参数不受影响）。

- [ ] **Step 4: 跑绿 + 回归**

Run: `pytest tests/unit/memory/ tests/unit/cognitive_layers/ -q`
Expected: 全绿；既有 `test_muscle_memory*` 若对 `_compute_confidence` 位置参数有精确断言，按新默认参数零破坏原则修测试构造而非改签名顺序。

- [ ] **Step 5: 提交**

```bash
git add neurova/cognitive_layers/memory_layer/muscle_memory.py tests/unit/memory/test_muscle_memory_semantic.py
git commit -m "feat(memory): T4 肌肉记忆语义置信 — MD5假向量升级为bge余弦分量(0.75~0.95线性档)，无向量/关开关逐位回落旧行为"
```

---

## Task T5: 参数落地校验（ungrounded 拒绝执行，默认关灰度）

**目的：** 移植 Needle 的"参数只含有依据的值"：日期字段（schema 标了 `"format": "date"/"date-time"` 的 string 参数）若年份在"本轮可见文本 + 系统时间事实"中无字面依据 → 判臆造、拒执行、回传 `ungrounded`。因 Neurova 有时间感知模块可合法推算日期（Needle 没有），**默认关**（`NEUROVA_ARG_GROUNDING=1` 显式开），属灰度能力非缺陷修复。

**Files:**
- Create: `neurova/security/arg_grounding.py`
- Modify: `neurova/tool_executor.py`（T2 校验块之后）
- Modify: `neurova/agent/chat_pipeline.py`（设 ContextVar 一处）
- Modify: `neurova/builtin_tools.py`（给收日历日期的 string 参数补 `"format"` 标记——以 `grep -n '日期\|deadline\|due' neurova/builtin_tools.py` 圈定，当前 `_BUILTIN_SCHEMAS` 实测 0 处 format，全部新增）
- Create: `tests/unit/security/test_arg_grounding.py`

- [ ] **Step 1: 写失败测试**

```python
"""T5 日期落地校验：年份无字面依据拒执行；有依据/非日期/开关关放行。"""
from neurova.security.arg_grounding import find_ungrounded_date_fields

_SCHEMA = {"type": "object", "properties": {
    "date": {"type": "string", "format": "date"},
    "meeting_at": {"type": "string", "format": "date-time"},
    "note": {"type": "string"},
}}


def test_ungrounded_year_detected():
    bad = find_ungrounded_date_fields(_SCHEMA, {"date": "2027-03-01", "note": "ok"},
                                      source_text="帮我记一下明天开会 2026年")
    assert bad == ["date: 值 2027-03-01 的年份 2027 在可见文本中无字面依据"]


def test_grounded_passes():
    assert find_ungrounded_date_fields(_SCHEMA, {"date": "2026-09-20"}, source_text="2026 财年计划") == []


def test_date_time_format_checked_too():
    bad = find_ungrounded_date_fields(_SCHEMA, {"meeting_at": "2031-01-01T10:00"}, source_text="今天 2026-09-13")
    assert bad and bad[0].startswith("meeting_at")


def test_non_date_field_ignored():
    assert find_ungrounded_date_fields(_SCHEMA, {"note": "2099"}, source_text="x") == []


def test_schema_markers_present():
    """至少一个内置工具的日期参数带 format 标记，否则本功能空转。"""
    from neurova.builtin_tools import _BUILTIN_SCHEMAS
    marked = [n for n, e in _BUILTIN_SCHEMAS.items()
              for p in (e["parameters"].get("properties") or {}).values()
              if p.get("format") in ("date", "date-time")]
    assert marked, "需按 Step 5 给收日历日期的参数补 format"
```

Run: `pytest tests/unit/security/test_arg_grounding.py -q` → FAIL（模块不存在 + 无 format 标记，两红各有实指）。

- [ ] **Step 2: 实现模块**

```python
# neurova/security/arg_grounding.py
"""参数落地校验（计划 T5，Needle grounding 移植）。默认关：NEUROVA_ARG_GROUNDING=1 显式开。

契约：schema 标了 format=date/date-time 的 string 参数，其年份必须出现在
"本轮可见文本"（ContextVar，由 chat_pipeline 每轮 set）中，否则判模型臆造、
拒执行回传 ungrounded。不做关键词猜测，纯字面年份比对，误伤面为零。
"""
import contextvars
import os
import re
from typing import Any, Dict, List

_YEAR_RE = re.compile(r"(?:19|20)\d{2}")
_source: contextvars.ContextVar[str] = contextvars.ContextVar("arg_grounding_source", default="")


def set_conversation_source(text: str) -> None:
    _source.set(text or "")


def get_conversation_source() -> str:
    return _source.get()


def grounding_enabled() -> bool:
    return os.environ.get("NEUROVA_ARG_GROUNDING") == "1"


def find_ungrounded_date_fields(schema: Dict[str, Any], arguments: Dict[str, Any],
                                source_text: str) -> List[str]:
    props = (schema or {}).get("properties") or {}
    years = set(_YEAR_RE.findall(source_text or ""))
    out: List[str] = []
    for key, spec in props.items():
        if not isinstance(spec, dict) or spec.get("format") not in ("date", "date-time"):
            continue
        value = (arguments or {}).get(key)
        if not isinstance(value, str) or len(value) < 4:
            continue
        year = value[:4]
        if not re.fullmatch(r"(?:19|20)\d{2}", year):
            continue
        if year not in years:
            out.append(f"{key}: 值 {value} 的年份 {year} 在可见文本中无字面依据")
    return out
```

- [ ] **Step 3: 接线执行器（T2 校验块之后、`# 方案 P0-1.5` 治理注释之前）**

```python
            # 工具链升级 T5：日期臆造防线（默认关，NEUROVA_ARG_GROUNDING=1 开启）
            from neurova.security.arg_grounding import grounding_enabled, find_ungrounded_date_fields, get_conversation_source
            from neurova.security.tool_arg_validator import get_tool_arg_schema

            if grounding_enabled():
                _g_schema = get_tool_arg_schema(tool_name)
                if _g_schema is not None:
                    _ungrounded = find_ungrounded_date_fields(_g_schema, params, get_conversation_source())
                    if _ungrounded:
                        result = {
                            "success": False,
                            "error": "参数落地校验未通过: " + "; ".join(_ungrounded),
                            "validation": {"ungrounded": _ungrounded},
                        }
                        return result
```

- [ ] **Step 4: 接线 chat_pipeline（每轮设可见文本）**

插入点为 `neurova/agent/chat_pipeline.py:1683`，`_step_llm_call` 内 `tools_for_llm = await self.context_orchestrator.build_tools_for_llm()` 行的紧下方（此处 `ctx.context` 已定型含 system 注入，:1731 注释实据）：

```python
        # T5：本轮可见文本 = system 轮 + 最近 8 条消息内容，供日期落地校验。
        # system 轮含注入的当前日期时间，即 Needle 式"system facts 许可"依据面。
        try:
            from neurova.security.arg_grounding import set_conversation_source

            _msgs = ctx.context or []
            _lead = [_msgs[0]] if _msgs and _msgs[0].get("role") == "system" else []
            set_conversation_source("\n".join(
                str(m.get("content", "")) for m in _lead + _msgs[-8:]))
        except Exception:  # noqa: BLE001 - 防线数据注入失败不阻断对话
            pass
```

ContextVar 在同一 asyncio 任务链内对下游 `_execute_single_tool` 可见（与项目 ContextVar 作用域机制同构，见技能记忆 [[neurova-isolation-audit-fix-status]] 的 ContextVar 契约）。

- [ ] **Step 5: 补 format 标记 + 跑绿**

`grep -n '日期\|deadline\|due' neurova/builtin_tools.py` 圈定收日历日期的 string 参数，逐个加 `"format": "date"`（纯 schema 元数据追加，OpenAI 通道透传无副作用）。
Run: `pytest tests/unit/security/test_arg_grounding.py tests/unit/security/test_tool_arg_validator.py -q` → 全绿。

- [ ] **Step 6: 提交**

```bash
git add neurova/security/arg_grounding.py neurova/tool_executor.py neurova/agent/chat_pipeline.py neurova/builtin_tools.py tests/unit/security/test_arg_grounding.py
git commit -m "feat(tools): T5 日期参数落地校验 — schema format标记+年份字面依据比对，默认关灰度(env)"
```

---

## Task T6: 工具面验收集（冻结契约，防未来漂移）

**目的：** 借 Needle environments"冻结验收套件"概念：把"用户话术 → 期望工具调用"沉淀为 JSON 夹具，钉三件事——工具名在 dispatch/schema 存在、期望参数过 T2 校验器、schema 合法（复用 T1 机制）。新工具上线必须带夹具条目。

**Files:**
- Create: `tests/unit/tools/fixtures/tool_surfaces/weather_file_memory.json`
- Create: `tests/unit/tools/test_tool_surface_acceptance.py`

- [ ] **Step 1: 写夹具**

```json
{
  "surface": "weather_file_memory",
  "records": [
    {"query": "北京现在多少度", "answers": [{"name": "weather", "arguments": {"city": "北京"}}]},
    {"query": "把这段存进记忆", "answers": [{"name": "memory_search", "arguments": {"query": "存记忆"}}]},
    {"query": "读一下 readme 第一行起", "answers": [{"name": "file_read", "arguments": {"file_path": "README.md", "offset": 1}}]},
    {"query": "刚才聊的那件事帮我找回来", "answers": [{"name": "recall_history", "arguments": {"query": "那件事", "limit": "5"}}]}
  ]
}
```

（`limit: "5"` 故意字符串化，钉死 T2 宽容归一路径。）

- [ ] **Step 2: 写失败测试（夹具校验器）**

```python
"""T6 工具面验收集 — 每条记录：工具名存在、期望参数过 schema+T2 校验器。"""
import json
from pathlib import Path

import pytest
from jsonschema import Draft202012Validator

from neurova.builtin_tools import _BUILTIN_SCHEMAS
from neurova.security.tool_arg_validator import validate_tool_args

_FIXTURES = sorted((Path(__file__).parent / "fixtures" / "tool_surfaces").glob("*.json"))


def test_fixtures_exist():
    assert _FIXTURES, "至少一份工具面验收夹具"


@pytest.mark.parametrize("path", _FIXTURES, ids=lambda p: p.name)
def test_surface_records_contract(path):
    doc = json.loads(path.read_text(encoding="utf-8"))
    for rec in doc["records"]:
        for call in rec["answers"]:
            name = call["name"]
            assert name in _BUILTIN_SCHEMAS, f"{path.name}: 工具 {name} 不在 schema 单源"
            entry = _BUILTIN_SCHEMAS[name]
            Draft202012Validator(entry["parameters"]).validate(call["arguments"])
            _, errors = validate_tool_args(name, call["arguments"])
            assert not errors, f"{path.name}/{name}: {errors}"
```

- [ ] **Step 3: 跑红→绿→全量回归**

先跑 `pytest tests/unit/tools/test_tool_surface_acceptance.py -q`：若红，按报错修夹具（工具名拼错/参数非法都是真发现，记入提交说明）。绿后跑收尾回归：

```bash
pytest tests/unit/tools tests/unit/security tests/unit/context tests/unit/memory tests/unit/agent tests/unit/test_tool_bugs_v2.py tests/unit/test_tool_bugs_v3.py -q
```
Expected: 全绿。**验收后跑一次后端全量 unit 套件**（`pytest tests/unit -q`），与修复前基线对比不许新增失败。

- [ ] **Step 4: Live-verify（默认开的新闸真实链路验收）**

启动后端 `python start.py --backend`，用 REPL（`cli.py`）发一轮会诱导字符串参数的工具调用（如"查一下北京天气"），确认日志无 `param_errors`；再人工构造非法参数轮（REPL 里让模型对 file_read 传 `{"file_path": 123}` 类诱导，或直连 `ToolExecutor.execute` 脚本调用）确认返回 `param_errors` 且下一轮模型自纠成功。

- [ ] **Step 5: 提交 + 台账登记**

```bash
git add tests/unit/tools/
git commit -m "test(tools): T6 工具面冻结验收集 — 话术→期望调用JSON夹具，钉schema/T2校验器/dispatch三契约"
```

在 `docs/` 的资源型台账或本计划文档末尾登记：T1 红灯期逐违规裁决清单（哪个工具、选了哪个修法）。

---

## 实施顺序与依赖

```
T1（独立，最先——它可能改 schema，T2/T5/T6 消费其终态）
T2（依赖 T1 稳定）
T3（独立，只依赖 embedding 引擎）
T4（独立）
T5（依赖 T2 的 get_tool_arg_schema 查表）
T6（依赖 T2 完成，最后收尾回归）
```

T3/T4 与 T1/T2 无代码耦合，可并行会话实施；T2/T5/T6 必须串行。

## 风险与对策

| 风险 | 对策 |
|---|---|
| T2 上线误伤存量"多传参数/弱参数"工具 | 未知键放行 + 宽容归一；回归 Step 7 逐条判定不许 skip；`=0` 秒回滚 |
| T4 嵌入使旧条目分数变化导致自动执行阈值抖动 | 旧条目无向量=逐位回落（单测钉死）；新条目余弦映射区间 0.75~0.95 保守设计，低于 0.75 不给分 |
| T3 首次全目录嵌入延迟（62 工具 × ~20ms ≈ 1.2s 一次性） | 索引持久化后仅增量；发生在 A6 激活态，而 A6 本就默认关 |
| T5 误伤合法时间推算（如"下周三"） | 默认关 + 纯字面年份比对；灰度先只观察日志不接执行闸（可先以 logger.warning 模式发布一轮） |
| Phase 0 路径加 encode 的时延（T4） | `_semantic_vector` 失败/缺失返回 None 不阻断；实测 bge ONNX CPU encode 10-50ms，对 LLM 秒级链路无感 |

## 范围外（明确不做，留档理由）

- **接入 Needle 模型本体**——已决策不做（能力包线窄、中文未验证、置信头微调失效）。
- **文法约束解码（xgrammar/outlines 类）**——仅本地 vLLM 通道可用，云端 provider 不支持，收益面太小。
- **`@tool` 装饰器自动派生 schema 全面迁移**——T1 护栏先止住漂移；迁移 62 工具是独立的纯重构工程，单独立项。
- **Tool Search 默认开启翻转**——属行为面决策，待 T3 语义版实测检索质量数据后另行拍板。
- **LoRA 微调工具调用模型**——Neurova 有轨迹数据（TrajectoryRecorder）可导 Needle JSONL 格式，但训练栈引入成本 > 现收益，挂起。

## 最终验收清单

- [ ] T1 三测全绿且红灯裁决清单登记进台账
- [ ] T2 非法参数轮：模型收到逐字段错误并自纠成功（REPL 实录）
- [ ] T3 真 bge 引擎冒烟：词面不交的近义查询命中正确工具
- [ ] T4 近义改写命中记忆条目，`NEUROVA_MUSCLE_SEMANTIC=0` 分数逐位等旧
- [ ] T5 开 env 后臆造日期被拒执行；关 env 零行为
- [ ] T6 全量 unit 回归零新增失败；`python start.py --check` 正常
- [ ] 每项均有独立提交，可单项 revert 不连坐

---

## 实施勘误（2026-09-13 实施实录，计划初稿与实况的偏差）

| 任务 | 计划假设 | 实况与落地调整 |
|---|---|---|
| T1 | `_method_ast` 用 `ast.FunctionDef`、dispatch 正则 `=\s*\{` | 执行体全为 `async def` → 改 `(FunctionDef, AsyncFunctionDef)`；dispatch 带 `: Dict[str, str]` 注解 → 正则越过注解。已按修正版入库 |
| T2 | 红灯来自执行体读未声明参数 | 另抓到 **weather/web_search 存量契约 bug**：`required` 单指主参数与"二选一"描述矛盾（T2 校验上线即暴露，闭环测红）→ 修复为 `anyOf` 三选一 + 3 枚回归用例 |
| T5 | 给收日历日期的内置参数补 `"format"` 标记 | grep 全仓实证：**当前 62 内置工具面无一收日历日期参数**（调度器为内部模块不进 LLM 面）。不造锚点，落地为 **inert-but-ready**：机制/双接线/11 测全交付，format 标记本身即生效开关，未来日期工具标 format 即自动生效；另补 executor 接线钉测（伪造标记 schema 验拒执行/放行双向） |
| T6 | 假想红灯数 | 全量对照须走干净 worktree（`git worktree add ../x HEAD` 同 venv 复跑 diff 失败集），直接工作区跑会混入并行会话预存红（zoom_out 3 红、test_tool_bugs_v2/v3 8 红、context_pool 4 红均系并行改动预存） |

台账（T1 红灯期逐违规裁决）：`computer_scroll` x/y、`computer_type` interval、`run_code` cwd/env/runtime_type → **补 schema 声明**（归一白名单 `_COMPUTER_TOOL_PARAM_KEYS`/执行体 docstring 双实据证明系 schema 落后）；`get_datetime` tz → **豁免登记**（`params.get("timezone") or params.get("tz")` 兼容别名，双名进 schema 反诱导模型混用）；`execute_code`/`search` → **别名登记**（正规名方法的第二入口）。
