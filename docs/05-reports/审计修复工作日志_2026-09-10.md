# 审计修复工作日志（2026-09-10 夜间自动执行）

- **执行者**：sess_f7e8bf60（夜间自主批次）
- **依据**：`docs/05-reports/源码全面审计_2026-09-10.md` + `docs/04-plans/audit-remediation-plan-2026-09-10.md`
- **约束**：并行会话 sess_da7e7cec（Kai 恢复 + /compact）在同一工作区活动，按文件清单隔离，提交全部走 pathspec

## 提交清单（8 个）

1. `c326bc8b` P0-A 安全收口八连（测试先行：test_audit_p0a_security.py 20 用例）
2. `7487a18e` A1 探测函数类/实例双形态兼容 + 沙箱契约测试同步
3. `a0500416` P0-B 并发六连 + core/turn_context.py 新模块（test_audit_p0b_concurrency.py 7 用例）
4. `d48a5c51` P0-C 真流式四连（test_audit_p0c_streaming.py 5 用例）
5. `3820cdd9` P1-D 记忆层六连（test_audit_p1d_memory.py 7 用例）
6. `da1dba2a` P1-E 扫荡（E1/E5/E6/E7）
7. `04f8f186` P1-F 假接线（F1/F3/F4/F7）
8. `27bd4f95` P1-G 前端（G3/G5）
9. `141ed493` P2-H 清理（H4/H5/H6/H7）

## 关键技术决策

- **B1 轮次态 ContextVar**：新建 `neurova/core/turn_context.py`（7 个 ContextVar），Agent 轮次级 API 改读写它，旧实例属性名经 property 透传；tool_executor 三处 `agent._tool_messages_list` 直写改 `append_tool_messages` 公有 API
- **C1 真流式**：`/chat/stream` 复用 console 的 emitter→queue 模式，`agent.chat(stream=True)` 后台任务 + content/reasoning 事件即时推送；无 content 时用最终回复兜底（命令回复场景）
- **C3 重试单层**：`LLMConfig.max_retries` 默认 3→0（SDK 直传），重试语义归外层 RetryConfig 单层
- **D1 WAL**：persist.db 常驻连接（check_same_thread=False + RLock）+ WAL + synchronous=NORMAL；`_persist_memory` 走常驻连接，降级路径保留
- **D6 索引增量**：semantic_search 新增 `upsert_memory_index`/`remove_memory_index`，manager.remember/forget 挂钩子；recall 路径加"空索引才全量建"守卫
- **F4 会话定位**：`SessionManager.find_session` 文件名 glob 直配，console 4 端点替换（delete/rename/auto-title/round-ops）
- **A1 探测表**：兼容类与实例两种表条目（P1-7 存量测试以实例注入为契约）

## 测试

- 新增 39 用例（P0-A 20 + P0-B 7 + P0-C 5 + P1-D 7），全部先红后绿
- 契约同步 9 个存量测试（BUG-7 注入点、state_api self-heal、turn_session 直写、hot_switch 桩、auto_title/fork 桩、T4 断言、agents store 缓存、streaming stub 属性）
- 基线比对方法：`git worktree add` 干净 HEAD 跑同套件比对失败清单（llm 28、memory 3、evolution 1 全预存）

## 事故：stash 误弹（02:02）

`git stash push neurova/tool_executor.py` 因该文件只有暂存态改动而未保存任何内容，随后 `git stash pop` 把预存的旧快照 stash（"filter-branch: rewrite"，无 parents）弹进工作区：冲掉 A3/A4/A5 编辑、带入坏 `security/__init__.py`（硬 import 不存在的 api_keys）、扩散 361 个 02:02:55 同秒创建的旧文件（neuUI/、meta_cognition_layer、knowledge/rag 等）。

处置：五文件 checkout HEAD 后干净重放；`security/__init__.py` 还原 HEAD；361 文件按 mtime==1788976975 精确判定删除（另一会话合法文件时戳分散保留）；弹入 stash 可经 reflog e1c28825 恢复。

**教训（已写入记忆）**：共享工作区禁用 stash push/pop（空 push + pop 会弹错东西）；基线比对一律用 `git worktree`。

## 遗留

后置台账 12 项（F6/F2/F5/E2/E3/E4/D3/D4/D5/G1/G2/G4/G6/G9/H1/H2/H3）与原因写入计划文档"执行状态"节；预存环境问题 6 条（Pillow DLL、pydantic v1、缺库×4、工作区残留、顺序污染、canvas-edges）。


---

## 增补：两起事故晨间复核（2026-09-10 白天，只增不覆盖原则）

### 事故一定性修正 + 遗留收尾

**定性修正**：夜间报告称"弹入 filter-branch 时代的旧快照"不准确。经 `git show e1c28825` 核实，被弹出的 stash 是**另一会话 02:02:17 刚创建的工作区快照**（父提交即当时 HEAD 23246f7d，tracked 部分=我的 P0-A 编辑+它的前端改动；untracked ^3 树仅 3 个测试文件）。工作日志原节保留不改，此处为修正口径。

**361 个被删文件的最终甄别**（全部逐个验证，非抽样）：
- 内容与 stash@{1}（6月5日 WIP）/stash@{2}（6月4日 WIP）树逐字节一致 → 复活源是**6月旧 stash**，非另一会话的文件
- 关键 import 断链甄别：`neurova/evolution/tool_weights.py`、`conflict_detector.py`（v1）、`multi_agent_sleep_manager.py` 虽被 integration 测试引用，但 **HEAD 均不跟踪**且对应测试带 `pytest.skip` 守卫（docs/test-debt-skip-list.md 在案的预存债务）——删除未造成任何现行代码断链
- `neuUI/`、`auth.py`、`module_lib.py`、`app_restored.py`、`context_legacy.py`：git 历史明确删除/测试断言必须不存在 → 僵尸复活，删除正确

**找回兜底**：361/361 全量副本已从旧 stash 树提取至 `.stash-recovery-20260910/`（已加 .gitignore，不入库不进工作区），供任何需要的审计比对。stash@{0}（另一会话 02:02 快照的同 message 条目）与 stash@{1..3} 未动。

### 事故二定性修正 + 恢复结果

**定性修正**：MEMORY.md 的混合编码**并非夜间操作引入**——并行会话 18:55:10 修改时即遭遇同一 `UnicodeDecodeError 0x9e`（其记录在案，用 surrogateescape 完成）；夜间 GBK 全文解码把损坏扩大为可见乱码。

**恢复结果**：
1. 索引 147 条从 147 个记忆文件的 frontmatter description 完整重建（description 即旧钩子的数据源）
2. 8 条人工精修钩子 + 1 条夜间新增，从并行会话 transcript（msg[244/245/615] 的 Read/Edit 原文）**原样恢复**——含并行会话最后一次写入的 context-compaction 最终版
3. 等价性抽验 8 条：旧索引钩子与 description 逐字同源（mismatch=0）——除 8 条精修行外信息无损
4. 无法恢复的部分：无（9 条精修+138 条 description 同源覆盖全部 147 条）

### 操作规程补充（写入记忆）

- 共享工作区：禁用 `git stash push/pop`；基线比对用 `git worktree add /tmp/x HEAD`
- MEMORY.md 为混合编码文件：修改一律走 `io.open(errors='surrogateescape')` 或逐行原样保留策略，禁全文件解码重写
