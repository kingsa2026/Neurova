"""diagnostics 包 —— 编程过程临时定位/检查脚本的统一收纳与索引。

包含:临时探针(_probe/_agent_probe/_trace_stack)、本地后端启动(_run_backend)、
lint 报告分析(analyze_issues)、数据库检查(check_databases/check_users_db/check_and_start)、
语法批量修复(fix_*)。完整索引见项目根 README 或 scripts/diagnostics/INDEX.md。
"""